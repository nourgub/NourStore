# قائمة التحقق الموحّدة قبل الإطلاق التجاري — NouriX Academy

> هذه القائمة هي المرجع الوحيد لتحديد جاهزية الإطلاق. لا يُعتبر المشروع
> "جاهزًا للسوق" حتى تتحول كل بنود القسم "إلزامي" أدناه إلى ✅ عبر تشغيل
> فعلي للأمر/الاختبار المذكور — وليس بالقراءة أو الافتراض. آخر تحديث:
> 2026-09-01. راجع `PHASE1_STATUS.md` لتفاصيل كل بند مُنجَز.

## كيفية الاستخدام
- ✅ = تم التحقق فعليًا بالتشغيل في هذه الجلسة أو جلسة سابقة موثّقة.
- ⬜ = لم يُنفَّذ أو لم يُتحقَّق منه بعد — لا تُعلن الجاهزية قبل إغلاقه.
- كل بند يذكر **كيفية التحقق منه** (أمر أو اختبار)، وليس فقط الوصف.

---

## إلزامي — لا إطلاق تجاري بدونه

### الجودة الأساسية وقابلية التحقق
- [x] `pnpm run check` (type-check) ينجح بلا أخطاء.
- [x] `pnpm run build` (production) ينجح.
- [x] `pnpm test` (بدون `DATABASE_URL`) ينجح بالكامل — 197 اختبارًا حاليًا.
- [x] اختبار E2E حقيقي ضد MySQL فعلية للمسار الكامل (تسجيل → دورة →
      درس → اختبار → شهادة → تحقق) — `server/realDb.e2e.test.ts`، يُشغَّل
      عبر `DATABASE_URL=... pnpm test server/realDb.e2e.test.ts`.
- [x] جميع الـ19 migration تُطبَّق بالترتيب على قاعدة فارغة بدون خطأ —
      `node scripts/migrate.mjs`.
- [x] تحقق صريح من متغيرات البيئة الحرجة عند بدء الخادم (`JWT_SECRET`
      قوي، `DATABASE_URL` في الإنتاج) — `server/_core/env.ts`.
- [x] Health check حقيقي يفحص قاعدة البيانات فعليًا — `/api/health`.
- [x] **إصلاح حرج: تسجيل الدخول بالبريد الإلكتروني كان مكسورًا بصمت على
      أي اتصال HTTP عادي (غير HTTPS)** — اكتُشف فقط بمحاولة تسجيل دخول
      حقيقية عبر متصفح حقيقي أثناء المراجعة البصرية على الهاتف؛ الخادم
      كان يُرجع نجاحًا (`{"ok":true}`) لكن المتصفح يرفض تخزين ملف
      تعريف الارتباط بصمت (`SameSite=None` بلا `Secure=true` — مزيج
      غير صالح وفق مواصفة المتصفحات الحديثة). يعني عمليًا: **أي نشرة
      ذاتية الاستضافة لم تُفعِّل HTTPS بعد كانت ستمنع كل تسجيلات
      الدخول بالبريد الإلكتروني بصمت تام**، وهو تحديدًا سيناريو شركة
      ناشئة جزائرية قبل ربط نطاق وشهادة SSL. **أُصلح ومُختبَر بـ7
      اختبارات وحدة + تسجيل دخول حقيقي كامل بعد الإصلاح نجح فعليًا**
      (انظر تفصيل كامل في قسم "المحتوى والشهادات" أدناه).
- [x] **`pnpm test` يعمل الآن بأمان تام مع `DATABASE_URL` حقيقية —
      أُغلق، وبإصلاح أدق بكثير مما كان مُفترَضًا سابقًا**. عند التشغيل
      الفعلي الكامل مع قاعدة بيانات حقيقية، تبيَّن أن المشكلة الحقيقية
      كانت **اختبارًا واحدًا فقط من أصل 234** (`whatsappPayments.test.ts`
      → `setWhatsappSession`) — وليس "بنية الاختبارات القديمة (197)"
      بأكملها كما وُصفت سابقًا. السبب: هذا الاختبار الوحيد يحتوي عملية
      **كتابة** (INSERT) بمعرِّف فاتورة ثابت غير موجود، بينما بقية
      اختبارات "no DB" الأخرى (فُحصت جميعها لتأكيد ذلك) إما عمليات قراءة
      فقط (تُرجع نتيجة فارغة سواء القاعدة غير موجودة أو حقيقية-لكن-فارغة
      من بيانات الاختبار) أو اختبارات رفض مصادقة مستقلة تمامًا عن طبقة
      قاعدة البيانات. **الإصلاح**: `describe.skipIf(!!DATABASE_URL)`
      على الاختبار الوحيد المعنيّ فقط — يُسقَط بوضوح (وليس فشلًا صامتًا)
      عند وجود قاعدة حقيقية، لأنه يختبر تحديدًا سلوك "لا قاعدة بيانات"
      تحديدًا. **مُتحقَّق منه فعليًا في الاتجاهين**: مع `DATABASE_URL`
      → 231 نجح، 3 أُسقطت بوضوح، **صفر فشل** (كان فشل واحد سابقًا)؛
      بدونها → 216 نجح، 18 أُسقطت بوضوح (لم يتغيّر). لم تكن هناك حاجة
      لإعادة هندسة معمارية كبيرة (transactions/rollback) كما افتُرض.
- [ ] **تحديث لاحق (نفس اليوم)**: اكتُشف **تذبذب (flakiness) جديد
      وغير مُصلَح** عند تشغيل `pnpm test` الكامل (كل الملفات معًا) مع
      `DATABASE_URL` حقيقية تحديدًا — أحيانًا 3 اختبارات تفشل، وأحيانًا
      أخرى (بنفس قاعدة البيانات المُهيَّأة من الصفر) تنجح كلها. **تشخيص
      دقيق أثبت أن السبب غير متعلق بأي تعديل حديث**: ملف الاختبار
      المتأثر (`feature.contracts.test.ts`) **ينجح دائمًا وباستمرار
      100%** عند تشغيله منفردًا؛ الفشل يظهر فقط أحيانًا عند تشغيله
      **بالتوازي** مع ملفات اختبار أخرى ضمن عمال (workers) متعددين
      يتشاركون نفس قاعدة البيانات الحقيقية الواحدة — تسابق بيانات
      (race condition) بين ملفات مختلفة، وليس خللًا داخل ملف واحد. هذا
      نطاق مختلف عن الخلل المُصلَح أعلاه (ذاك كان حتميًا 100% في كل
      مرة؛ هذا عشوائي التوقيت). **لم يُصلَح** — يحتاج إما تعطيل التنفيذ
      المتوازي عند استخدام `DATABASE_URL`، أو معرِّفات فريدة لكل ملف
      اختبار بدل قيم ثابتة، أو مخطط اختبار منفصل لكل عامل. **البوابات
      الأساسية للمشروع غير متأثرة**: `pnpm test` بدون `DATABASE_URL`
      (الاعتماد الأساسي طوال هذا المشروع) و`realDb.e2e.test.ts` منفردًا
      كلاهما **ناجحان دائمًا وباستمرار** بلا أي تذبذب، وهما ما اعتمد
      عليه التحقق من كل ميزة أُضيفت.
- [x] **خلل `scripts/migrate.mjs` — أُصلح ومُتحقَّق منه فعليًا**: كان
      كشف "مُطبَّق مسبقًا" يعتمد على تخمين رسائل أخطاء SQL (فشل مع
      `DROP INDEX` في migration 0017). أُعيد بناء السكريبت باستخدام
      جدول تتبع حقيقي (`_migrations`) بدل التخمين، مع منطق تهيئة آمن
      للنشرات الحالية (يكتشف مخطط موجود مسبقًا بلا جدول تتبع، ويُسجّل
      الترحيلات كمُطبَّقة دون إعادة تنفيذ SQL). **مُختبَر فعليًا بأربعة
      سيناريوهات حقيقية** ضد MySQL/MariaDB: قاعدة فارغة (21/21) ·
      إعادة تشغيل فورية (0 مطبَّق، 21 مُتخطَّى بأمان — بالضبط الحالة
      التي كانت تفشل سابقًا) · قاعدة "قديمة الطراز" بلا جدول تتبع
      (تهيئة آمنة تلقائية) · إضافة migration جديد على قاعدة محدَّثة
      (يُطبَّق الجديد فقط). Phase 4 → أُغلق الآن.

### الأمان
- [x] لا أسرار أو مفاتيح API مكتوبة مباشرة في الكود (تم فحص شامل
      بالبحث عن أنماط المفاتيح الشائعة — لا نتائج).
- [x] `.env.example` يحتوي قيم توضيحية فقط، لا قيم حقيقية.
- [x] `passwordHash`/`answerKey` لا يظهران خارج نطاقهما الآمن إطلاقًا —
      تحقق شامل بالبحث في كل المشروع (Phase 6).
- [x] **إصلاح خطير**: روابط الملفات المحلية (إيصالات دفع، مرفقات دروس
      مدفوعة) كانت بلا أي حماية إطلاقًا — رابط ثابت يعمل للأبد لأي شخص
      يعرفه. أُضيفت نقطتا نهاية محميتان تُعيدان التحقق من الصلاحية في
      كل طلب (Phase 6).
- [x] **إصلاح خطير**: webhook واتساب الفعلي كان بلا تحقق توقيع إطلاقًا
      — أُضيف تحقق HMAC-SHA256 حقيقي مطابق لمواصفات Meta، مُختبَر
      بـ7 اختبارات (Phase 6).
- [x] webhook الدفعات: أُصلح قصور موثَّق ذاتيًا (توقيع على جسم مُعاد
      التسلسل بدل البايتات الخام الحقيقية) (Phase 6).
- [x] التحقق من رفع الملفات (نوع/حجم/امتداد/مسار): عميق فعليًا — تحقق
      متبادل امتداد↔MIME، حظر امتدادات تنفيذية حتى مُخفاة، تحقق حجم
      البايتات الفعلي، منع path traversal، **وتحقق حقيقي من البصمة
      الفعلية لمحتوى الملف (magic bytes)** لكل الصيغ الثنائية المدعومة
      (PDF/PNG/JPEG/WEBP/ZIP/MP4/WEBM) — يرفض الآن ملفًا مُعاد تسميته
      بامتداد مطابق لكن محتواه الفعلي مختلف. مُختبَر بـ10 اختبارات
      جديدة (16 إجمالًا) تشمل بايتات حقيقية وملفات مزوَّرة عمدًا لكل
      صيغة (Phase 6 — أُغلق هذا القصور المتبقي).
- [x] rate limiting: تغطية جيدة على العمليات الحساسة (تسجيل، دخول، دفع،
      اختبارات، دعوات، رفع ملفات، تذاكر دعم) + أُضيف للتحقق العام من
      الشهادة (كان بلا حماية) (Phase 6).
- [x] idempotency الشهادات: قيد فريد حقيقي على مستوى قاعدة البيانات
      (حماية مزدوجة ضد أي سباق تزامن) (Phase 6).
- [x] idempotency الإشعارات: منطق de-dup حقيقي موجود مسبقًا، تحقَّق منه
      (Phase 6).
- [x] عدم تسجيل بيانات حساسة: لا كلمات مرور/رموز في أي سجل، تحقَّق
      بالبحث الشامل (Phase 6).
- [x] **نسخ احتياطي واسترجاع حقيقيان**: لم يكونا موجودين إطلاقًا —
      أُضيفت سكريبتات فعلية، **تحقَّقت من دورة كاملة فعلية** (نسخ ثم
      استرجاع) ضد MySQL/MariaDB حقيقية (42 جدولًا مطابقة) (Phase 6).
- [x] سجل أخطاء قابل للبحث فعليًا — كان بلا بحث نصي إطلاقًا (فقط تصفية
      بالحالة)، أُضيف بحث حقيقي (Phase 6).
- [x] **مراجعة شاملة آلية (وليست عيّنات فقط) لصلاحيات/ملكية 54 نقطة
      نهاية** (كل إجراءات المتعلم + إجراءات المعلم/المؤسسة التي تأخذ
      معرِّف مورد) — سكريبت حقيقي دائم في المشروع
      (`scripts/audit-endpoint-ownership.ts`)، تحقَّق فعليًا من قدرته
      على اكتشاف ثغرة IDOR حقيقية عبر حقن حالة مُعطوبة عمدًا. **صفر
      حالة مشبوهة**. النطاق المتبقي: إجراءات `adminProcedure` (إداريون
      موثوقون بالتصميم، أولوية أقل) ومنطق دوال قاعدة البيانات الداخلي
      نفسه (الفحص يغطي الموجّهات فقط) — انظر `PHASE6_ENDPOINT_AUDIT.md`
      للتفاصيل الكاملة وحدود هذا الفحص بصراحة.
- [x] مراجعة شاملة (وليست عيّنة فقط) لعدم تسجيل بيانات حساسة داخل
      لوحات الإدارة والخادم بأكمله — فُحص كل استخدام `console.*` في
      كامل `client/src` و`server` (باستثناء الاختبارات): لا كلمات
      مرور، لا رموز جلسة، لا رموز OAuth (تحقَّق تحديدًا من مسار تبادل
      رمز جوجل — يعيد خطأ عام 502 دون تسجيل جسم الاستجابة عند الفشل).
      `LearningFlows.tsx` تحديدًا لا يحتوي أي استدعاء `console` إطلاقًا.

### الدفع (السوق الجزائرية)
- [x] **الدفع المباشر عبر الحساب البريدي (CCP) برفع إيصال على الموقع
      مباشرة — ميزة جديدة كاملة، بدون الحاجة لواتساب إطلاقًا**. زر
      "الدفع عبر الحساب البريدي" في صفحة الأسعار يفتح لوحة تعرض تفاصيل
      الحساب (`payment_rib_details`، القابل للتهيئة من لوحة الإدارة
      أصلًا) ومرجع الفاتورة، مع نموذج رفع صورة حقيقي. **حماية حقيقية
      من إعادة استخدام نفس الصورة**: بصمة SHA-256 لمحتوى الملف الفعلي
      (وليس اسمه) مع **قيد فريد (UNIQUE) على مستوى قاعدة البيانات
      نفسها** — لا يمكن قبول نفس صورة الوصل مرتين إطلاقًا، سواء لإعادة
      محاولة نفس الفاتورة أو لخداع النظام بادعاء دفعتين من تحويل واحد
      حقيقي. يُعيد استخدام نظام مراجعة الإيصالات ولوحة الإدارة الموجودة
      أصلًا لإيصالات واتساب (بلا أي تعديل عليها). **مُختبَر بحالة E2E
      حقيقية جديدة** ضد MySQL فعلية: رفع ناجح، رفض قاطع لنفس الصورة عند
      إعادة الرفع لفاتورة أخرى، ورفض رفع إيصال لفاتورة يملكها مستخدم
      آخر. `pnpm run check`/`test`/`build` كلها ناجحة بعد الإضافة.
- [x] الدفع اليدوي عبر واتساب يعمل فعليًا (فاتورة حقيقية → إيصال →
      مراجعة إدارية → تفعيل اشتراك) — مُختبَر في `realDb.e2e.test.ts`.
- [x] رفض الدفع لا يُفعّل الاشتراك أبدًا (مُختبَر).
- [x] عِلّة `createInvoice` (فاتورة خاطئة عند نقرة مزدوجة) — **مُصلَحة**.
- [x] وضوح الأسعار بالدينار الجزائري، والسعر النهائي، والمدة — مُتحقَّق
      منه (Phase 5).
- [x] مرجع الطلب (`NX-INV-{id}`) في كل رسالة واتساب بلا استثناء —
      مُتحقَّق منه (Phase 5).
- [x] منع تفعيل الاشتراك إلا بموافقة إدارية حقيقية أو webhook بتوقيع
      HMAC مُتحقَّق منه فعليًا (`timingSafeEqual`) — مُتحقَّق منه
      (Phase 5).
- [x] **إصلاح خلل تزامن حقيقي**: تفعيل الفاتورة كان بفحص-ثم-كتابة غير
      ذرّي، قابل نظريًا لتفعيل مزدوج عند webhook مكرر (موثَّق فعليًا على
      منصة واتساب) — أُصلح بتحديث SQL شرطي ذرّي، ومُختبَر (Phase 5).
- [x] منع تكرار تفعيل نفس الفاتورة — مُختبَر فعليًا (استدعاء مزدوج على
      نفس الفاتورة يُبقي اشتراكًا فعّالًا واحدًا فقط) (Phase 5).
- [x] حالة "منتهية الصلاحية" للفواتير المعلَّقة طويلًا — لم تكن موجودة
      إطلاقًا (فاتورة معلَّقة تبقى كذلك للأبد)؛ أُضيفت عبر migration +
      مهمة مجدولة (Phase 5).
- [x] **إلغاء اشتراك حقيقي** — السياسة القانونية كانت تَعِد بإمكانية
      الإلغاء من اللوحة لكن الميزة لم تكن موجودة إطلاقًا في الكود (وعد
      كاذب فعليًا). بُنيت الميزة الحقيقية (عادلة، لا تسحب أيامًا
      مدفوعة)، ومُختبَرة (Phase 5).
- [x] BaridiMob غير جاهز فعليًا، ويُقرّ بذلك بصدق (لا محاكاة نجاح وهمي)
      — مُتحقَّق منه (Phase 1 وPhase 5).
- [ ] سياسة إلغاء واسترجاع **مُراجَعة قانونيًا فعليًا** — النص الحالي
      يُحيل عمدًا للقانون التجاري الجزائري دون أرقام مُختلَقة (الصواب
      المتحفّظ بانتظار محامٍ حقيقي)؛ يبقى بندًا يتطلب طرفًا قانونيًا
      خارج نطاق العمل البرمجي.
- [x] **حالة "مرفوض" حقيقية على مستوى الفاتورة — أُضيفت** بطريقة تحافظ
      على القرار التصميمي الأصلي (الفاتورة تبقى "معلّقة" للسماح بإعادة
      إرسال إيصال مُصحَّح بنفس المرجع). حقل `lastReceiptStatus` محسوب
      يعرض آخر حالة إيصال فعلية، والواجهة تعرض الآن "الإيصال مرفوض —
      أعد الإرسال" بوضوح بدل "قيد الانتظار" المُضلِّلة. **مُختبَر
      فعليًا بحالتين E2E جديدتين** (17 إجمالًا) ضد MySQL حقيقية: تحقق
      من `lastReceiptStatus="rejected"` بعد رفض إداري فعلي، و
      `lastReceiptStatus="approved"` بعد موافقة فعلية (Phase 5).

### المحتوى والشهادات
- [x] الشهادة تُصدَر تلقائيًا فقط بعد إكمال 100% من الدورة (وبعد نجاح
      الامتحان النهائي إن وُجد) — مُختبَر.
- [x] لا يظهر محتوى درس محمي/مقفل لمستخدم غير مصرَّح له — مُختبَر.
- [x] عزل كامل بين بيانات المستخدمين (Learner A لا يرى بيانات Learner B)
      — مُختبَر.
- [x] كل رابط/زر في الصفحات الخمس الأساسية (الرئيسية، الكتالوج، تفاصيل
      الدورة، عارض الدرس، اللوحة) ينفذ وظيفة حقيقية — تم فحص شامل
      وإصلاح روابط كانت ثابتة (hardcoded) لدورات غير مضمونة الوجود
      (انظر `PHASE2_STATUS.md`).
- [x] حالات loading/empty/error واضحة ومتمايزة بصريًا في الصفحات
      الأساسية الخمس (لم تكن موجودة قبل المرحلة 2).
- [x] استئناف التعلم يعيد المتعلم فعليًا لآخر درس تفاعل معه (حقل
      `resumeLessonId` حقيقي من الخادم، وليس رابطًا عامًا للدورة).
- [x] منع نشر دورة بلا محتوى أساسي (درس واحد على الأقل) — مُختبَر
      (Phase 4).
- [x] منع حذف دورة/وحدة/درس يحتوي بيانات متعلمين حقيقية (تسجيلات/تقدّم)
      — مُختبَر (Phase 4).
- [x] تسجيل audit log لكل عمليات حذف/نشر/أرشفة المحتوى — كانت غائبة
      تمامًا، أُضيفت (Phase 4).
- [x] تأكيد واضح قبل حذف أي محتوى (دورة/وحدة/درس) — كان حذف الدرس بلا
      تأكيد، أُصلح (Phase 4).
- [x] حالة مسودة/منشور/مؤرشف حقيقية (عمود `status`) بدل ثنائية فقط —
      Phase 4.
- [x] preview حقيقي قبل النشر (صلاحية محسوبة من هوية المستخدم في
      الخادم) — Phase 4.
- [x] دعم ترتيب حقيقي للوحدات/الدروس (أزرار ↑/↓، تبديل ذرّي وآمن) —
      كانت كل العناصر الجديدة تُنشأ بترتيب ثابت `0` بلا أي طريقة
      لتغييره (Phase 4).
- [x] حقول أهداف التعلم/المتطلبات المسبقة/الفئة المستهدفة — مُضافة
      للمخطط وموصولة بالكامل في الإنشاء والعرض العام (Phase 4).
- [x] دورة تجريبية حقيقية مناسبة للسوق الجزائرية (BEM، رياضيات) —
      `scripts/seed-demo-course-bem.ts`، محتوى صحيح تحقَّق يدويًا،
      منفصل تمامًا وغير منشور تلقائيًا، لا شهادات/آراء وهمية (Phase 4).
- [x] **التأكيد الصريح في واجهة الشهادة أنها "شهادة إتمام من NouriX
      Academy" وليست اعتمادًا رسميًا من وزارة التربية — أُضيف فعليًا**
      في مكانين: ملف PDF القابل للتنزيل/المشاركة نفسه (الأهم — هو
      الوثيقة الفعلية التي قد تُعرَض)، وصفحة التحقق العامة. **مُتحقَّق
      منه بصريًا وليس افتراضًا**: وُلِّدت شهادات PDF حقيقية بثلاث لغات
      وفُحصت فعليًا (`pdftotext` للإنجليزية/الفرنسية، تحويل لصورة
      وفحص بصري للعربية). **اكتُشف وأُصلح خلل حقيقي أثناء هذا التحقق**:
      إدراج الاسم اللاتيني "Nourix Academy" داخل الجملة العربية تسبَّب
      في ظهوره مقلوبًا ("ymedacA xiruoN") بسبب خلل انعكاس ساذج معروف
      وموثَّق مسبقًا في نفس الملف لحقول أخرى — أُصلح باستخدام "نوريكس
      أكاديمي" العربية بدل الاسم اللاتيني، وأُعيد الفحص البصري
      للتأكيد. مُختبَر بـ2 اختبار جديد (7 إجمالًا في هذا الملف) (Phase 3).
- [x] لا بيانات عملاء/آراء/شهادات وهمية في الواجهة — فحص نصي عميق
      إضافي (أنماط "testimonial"، تقييمات نجوم، أسماء وهمية شائعة مثل
      "John Doe"، إحصائيات مُختلَقة مثل "10,000+ طالب") عبر كل صفحات
      ومكوّنات الواجهة — **لا نتائج في كلا الفحصين**. تبقى معاينة بصرية
      فعلية لكل صفحة (وليست نصية) بندًا مفتوحًا يحتاج أدوات متصفح غير
      متوفرة في هذه البيئة.
- [x] **تعديل الأهداف/المتطلبات بعد إنشاء الدورة — أُنجز بالكامل الآن**.
      استُبدلت سلسلة `window.prompt()` (كانت ستحتاج 12 نافذة متتالية)
      بلوحة تعديل حقيقية داخل الصفحة: كل الحقول (العنوان×3، الوصف×3،
      الفئة المستهدفة×3، الأهداف×3، المتطلبات×3) قابلة للتعديل بحقول
      إدخال حقيقية مع أزرار حفظ/إلغاء واضحة. **مُختبَر فعليًا بحالة E2E
      حقيقية جديدة** (18 إجمالًا) ضد MySQL: تحديث كامل عبر الموجّه
      الفعلي، تحقق من تخزين الحقول الفارغة كـ`NULL` صحيح (وليس نصًا
      فارغًا) للحفاظ على منطق "لا يظهر قسم فارغ"، وتحقق أن صفحة الدورة
      العامة تعكس التحديث فورًا. type-check نجح مباشرة رغم تعقيد
      التعديل (إعادة هيكلة JSX لاستخدام `Fragment`) (Phase 4).

### التوثيق
- [x] `PHASE1_STATUS.md` يعكس الحالة الفعلية المُتحقَّق منها.
- [x] تصحيح الادعاءات الرئيسية في `AUDIT.md`/`DEPLOYMENT.md`/
      `IMPLEMENTATION.md` (عدد الاختبارات، وحذف/تصحيح ادعاء SQLite غير
      الموجود).
- [x] **`AUDIT.md` كان يحتوي إشارات SQLite وهمية متناثرة — أُزيلت
      بالكامل**. عند المراجعة الفعلية سطرًا بسطر تبيّن أن العدد الحقيقي
      كان أكبر بكثير من التقدير الأولي "6 إشارات": شمل قسمي "تحقق"
      مُختلَقين بالكامل عن اختبار وضع SQLite غير موجود (حُذفا)، وعدة
      جمل/أمثلة مفردة داخل فقرات أخرى صحيحة المضمون (صُحِّحت فرديًا).
      لا يوجد الآن أي ذكر لـ`db.sqlite`/`sqliteDb`/`DB_DRIVER` في الملف
      خارج ملاحظات التصحيح نفسها.

---

## غير إلزامي للإطلاق الأول لكن مهم (يُنصح بإغلاقه قريبًا)

- [x] قياس حجم JavaScript فعليًا — lazy loading كان ممتازًا مسبقًا
      لكل الصفحات؛ لا حاجة لإصلاح (Phase 7).
- [x] إصلاح طلب شبكة فاشل مضمون في كل تحميل صفحة (تحليلات بمتغيرات
      غير معرَّفة إطلاقًا) — أُصلح بتحميل مشروط حقيقي (Phase 7).
- [x] تحسين تحميل الخطوط (من `@import` بطيء إلى `<link preconnect>`)
      (Phase 7).
- [x] Open Graph / meta tags — كانت غائبة تمامًا، أُضيفت كاملة + تحديث
      ديناميكي حقيقي لكل صفحة دورة (بلا كشف محتوى محمي) (Phase 7).
- [x] caching حقيقي للملفات الثابتة (طويل الأمد للملفات المُموَّهة،
      قصير لـ`index.html`) — تحقَّق فعليًا بـ`curl -I` (Phase 7).
- [x] تخزين مؤقت من جهة العميل (`staleTime`) — كان معدومًا تمامًا
      (Phase 7).
- [x] `robots.txt` و`sitemap.xml` ديناميكي حقيقي — لم يكونا موجودين
      إطلاقًا؛ الـsitemap يعكس الدورات المنشورة فعليًا من قاعدة
      البيانات مباشرة، تحقَّق فعليًا بإنشاء دورة حقيقية ورؤيتها تظهر
      (Phase 7).
- [x] لا SSR ولا خدمة خارجية كبيرة أُضيفت (Phase 7 — التزام بالقيد).
- [x] contrast الألوان للنصوص الصغيرة الخافتة — محسوب فعليًا (WCAG) ومُصحَّح
      في 6 مواضع كانت فعليًا أقل من 4.5:1 (Phase 3).
- [x] `prefers-reduced-motion` وfocus states — تم التحقق، سليمان مسبقًا
      (Phase 3).
- [x] مراجعة النصوص العربية/الفرنسية/الإنجليزية بحثًا عن خلط أو نسيان —
      فحص آلي شامل، لا حالات حقيقية وُجدت (Phase 3).
- [x] labels النماذج — **أُغلق بالكامل الآن**: كانت النماذج العامة
      (تسجيل الدخول، البحث، الدعم، الأسعار) مُغطاة منذ Phase 3.
      **النماذج الداخلية للإدارة أُضيفت الآن أيضًا**: 94 حقل إدخال في
      `LearningFlows.tsx` (كان تقديري الأولي ~80، العدد الفعلي 97، منها
      3 كانت تملك aria-label بالفعل) — أُضيف `aria-label` حقيقي مطابق
      لنص كل حقل عبر سكريبت آلي دقيق (يتعامل بشكل صحيح مع التعبيرات
      الشرطية متعددة اللغات والأسطر)، **تحقَّق منه فعليًا**: type-check
      نجح فورًا بعد التطبيق بلا أي خطأ صياغة، وفحص آلي إضافي أكّد عدم
      وجود أي تكرار في `aria-label`. مُختبَر بـ216 اختبارًا + build +
      17 اختبار E2E حقيقي بعد التطبيق.
- [x] **خلل RTL في اتجاه الأسهم — أُصلح ومُتحقَّق منه بصريًا فعليًا،
      وليس افتراضًا**. اكتُشف متصفح Chrome حقيقي (Chrome for Testing)
      وPlaywright متوفران فعليًا في بيئة التنفيذ — استُخدما لتشغيل
      التطبيق الفعلي وأخذ لقطات شاشة حقيقية بالعربية والفرنسية، **أثبتت
      الخلل بصريًا وبشكل قاطع** (مثال: "Explorer les parcours" بالفرنسية
      كان يُظهر سهمًا لليسار بدل اليمين). أُنشئ مكوّن مركزي حقيقي
      `client/src/components/DirectionalArrow.tsx` (`ForwardArrow`/
      `BackArrow`) يتكيّف تلقائيًا مع الاتجاه، وطُبِّق بدقة (بعد تصنيف
      دلالي لكل سهم: أمام أم خلف) على **12 صفحة**: `Home.tsx` (5
      مواضع)، `CourseCatalog.tsx`، `CourseDetail.tsx` (3 مواضع)،
      `LessonViewer.tsx` (4 مواضع)، `AlgorithmLab.tsx`، `Auth.tsx`،
      `CertificateVerify.tsx`، `Dashboard.tsx` (3 مواضع)، `Legal.tsx`،
      `Pricing.tsx`، `Search.tsx` (موضعان)، `Support.tsx` (موضعان) —
      **23 موضعًا إجمالًا**. في 4 ملفات لم يكن متغيّر `dir` مُعرَّفًا
      أصلًا، أُضيف. **أُعيد التقاط لقطات شاشة بعد الإصلاح وتأكَّد
      بصريًا** أن الأسهم تشير الآن للاتجاه الصحيح بالفرنسية، وأن أسهم
      "رجوع" بقيت صحيحة كما كانت. **نطاق هذا الإصلاح لا يزال جزئيًا
      وصريحًا**: لوحة الإدارة الضخمة (`LearningFlows.tsx`) لم تُفحَص/
      تُصلَح بعد — أولوية أقل (مستخدمون داخليون).
- [x] **اكتشاف إضافي عبر الفحص البصري الفعلي (لم يظهر في أي فحص نصي
      سابق)**: قسم "لماذا هذا المسار؟" في `CourseDetail.tsx` كان يعرض
      نصًا إنجليزيًا ثابتًا بالكامل للمستخدمين الفرنسيين (الشرط كان
      يتحقق من العربية فقط ثم يسقط للإنجليزية مباشرة، متجاوزًا الفرنسية
      تمامًا). أُصلح بترجمة فرنسية حقيقية، **ومُتحقَّق منه بصريًا** بعد
      الإصلاح.
- [x] **فحص آلي إضافي عن نفس نمط الخلل** (تعبير ثنائي عربي/إنجليزي بلا
      فرع فرنسي) وجد 240 موضعًا محتملًا؛ أغلبها إيجابيات كاذبة (فحوصات
      `dir` أو حقول إدارة بلغة محدَّدة عمدًا). رُوجعت العيّنات العامة
      يدويًا وأُصلحت أخطاء حقيقية إضافية في `Support.tsx` (تنسيق تاريخ
      كان يستخدم `fr-FR` للإنجليزية أيضًا، نص "اختر تذكرة" بلا فرنسية)،
      `Pricing.tsx` (نص الكوبون، وحدة الأيام)، `AlgorithmLab.tsx`
      ("جاري التحميل")، و5 حالات مماثلة في `LearningFlows.tsx`.
- [x] **مراجعة بصرية يدوية على هاتف حقيقي** لعارض الدرس وحل الاختبارات
      — **أُنجزت، واكتُشف خلال المحاولة خلل حرج منفصل تمامًا وأخطر من
      المتوقَّع**: تسجيل الدخول بالبريد الإلكتروني كان **يفشل بصمت**
      بسبب `sameSite: "none"` مع `secure: false` على اتصال HTTP عادي —
      مزيج يرفضه أي متصفح حديث وفق المواصفة (RFC 6265bis)؛ الخادم يُرجع
      `{"ok":true}` بنجاح، لكن المتصفح **يتجاهل ملف تعريف الارتباط
      بصمت تام**، فيبقى المستخدم غير مسجَّل دخول فعليًا دون أي رسالة
      خطأ. **اكتُشف فقط بمحاولة تسجيل دخول حقيقية عبر متصفح حقيقي** —
      لم يكن ليظهر بأي فحص كود أو اختبار وحدة تقليدي. **الإصلاح**:
      `sameSite: secure ? "none" : "lax"` بدل القيمة الثابتة السابقة —
      "lax" آمن وكافٍ تمامًا لتطبيق أحادي المصدر (الواجهة والخادم من
      نفس الأصل، لا حاجة فعلية لـ"none" إطلاقًا). **مُختبَر بـ7 اختبارات
      وحدة جديدة** (`server/_core/cookies.test.ts`) تغطي كل تركيبات
      HTTP/HTTPS/بروكسي عكسي، **ومُتحقَّق منه بتسجيل دخول حقيقي كامل**
      عبر النموذج الفعلي بعد الإصلاح — نجح ووصل فعليًا للوحة التحكم.
      بعدها: **وصول فعلي كامل لعارض الدرس واختبار الوحدة الحقيقيين**
      بحجم هاتف (390×844) — نص عربي طويل يُغلَّف بشكل صحيح، معادلة
      رياضية بأرقام لاتينية داخل نص عربي تُعرض بترتيب bidi سليم، لا أي
      تجاوز أفقي (`scrollWidth === clientWidth === 390` في كلتا
      الصفحتين)، خيارات الاختبار وتحديد الإجابة يعملان بصريًا بشكل
      صحيح.
- [x] **اكتشاف حرج عبر أول معاينة حقيقية بحجم هاتف على الإطلاق لهذا
      التطبيق**: صفحة كتالوج الدورات كانت تُنتج **تجاوزًا أفقيًا حقيقيًا**
      على الهاتف (390px شاشة، لكن المحتوى يمتد فعليًا إلى 441px منطقي)
      — رأس الصفحة (الشعار + رابط "الرئيسية" + رابط "المختبر" + أيقونة
      بحث + مبدّل السمة + مبدّل اللغة بثلاثة أزرار) أوسع من الشاشة،
      فيندفع مبدّل اللغة (EN/FR/AR) خارج الحافة اليسرى ويصبح **غير
      قابل للرؤية أو النقر جزئيًا على أي هاتف حقيقي**. **شُخِّص السبب
      بدقة عبر استعلام DOM فعلي** (`getBoundingClientRect` لكل عنصر)
      وليس تخمينًا. **الإصلاح**: توسيع قاعدة CSS الموجودة لإخفاء رابط
      "المختبر" النصي أيضًا على الشاشات الضيقة (كانت تُخفي رابط
      "الرئيسية" فقط)، + إضافة `overflow-x: hidden` على `html`/`body`
      كشبكة أمان دفاعية عامة ضد أي تجاوز أفقي مشابه غير مُكتشَف بعد في
      صفحات أخرى. **مُتحقَّق منه رقميًا وبصريًا معًا بعد الإصلاح**:
      `scrollWidth === viewportWidth === 390` بالضبط (كان 441)، ولقطة
      شاشة جديدة تؤكد أن الرأس الآن محصور بالكامل داخل حدود الشاشة.
- [x] **معاينة بصرية فعلية لصفحة الشهادة كصفحة ويب — أُنجزت**. أُصدرت
      شهادة حقيقية (تسجيل مكتمل فعليًا 100% في قاعدة بيانات حقيقية عبر
      `issueCertificate`) وفُتحت صفحة `/verify/certificate/NX-...`
      الحقيقية بمتصفح حقيقي: **سطح مكتب بالعربية والفرنسية، وحجم هاتف
      حقيقي (390×844)**. **لا أي خلل بصري جديد اكتُشف** — التخطيط
      نظيف بكلتا اللغتين (RTL/LTR)، **التصريح القانوني المُضاف سابقًا
      ("ليست اعتمادًا رسميًا من وزارة التربية") يظهر بشكل صحيح تمامًا
      في كلتا اللغتين** (تأكيد بصري إضافي بعد التحقق السابق من نسخة
      PDF فقط)، سهم "العودة للرئيسية" صحيح اتجاهيًا في الفرنسية (يؤكد
      نجاح إصلاح `DirectionalArrow` في صفحة لم تُذكَر صراحة سابقًا)،
      و**لا تجاوز أفقي على الهاتف** (`scrollWidth === clientWidth === 390`
      بالضبط) رغم أن هذه الصفحة لم تُعدَّل مباشرة — دليل إضافي على نجاح
      شبكة الأمان العامة (`overflow-x: hidden`) المُضافة سابقًا.
- [x] **قياس أداء ميداني حقيقي (Lighthouse) — أُنجز، واكتُشف خلل CSS
      بنيوي منهجي خطير أثناء التحقق**. Lighthouse حقيقي (v13، عبر
      المتصفح الحقيقي المُكتشَف سابقًا) شُغِّل فعليًا بمحاكاة أداء
      الهاتف على الصفحة الرئيسية. **النتائج الأولية**: الأداء 68،
      الوصولية 88، أفضل الممارسات 96، السيو 91.
      **أثناء إصلاح فحوصات الوصولية الفاشلة (تكبير الشاشة، تعارض
      aria-label، تباين لوني)، اكتُشف خلل بنيوي خطير في CSS**: قواعد
      مثل `[data-theme="light"] .X, .Y` — الفاصلة تفصل محدِّدَين
      **مستقلَّين تمامًا**، فالجزء الثاني (`.Y`) **غير مُقيَّد بالسمة
      الفاتحة إطلاقًا** ويتسرّب ليُطبَّق على كل السمات، بما فيها
      الداكنة. **فحص آلي شامل للملف بأكمله كشف 16 حالة إضافية** من نفس
      النمط بالضبط — نمط منهجي متكرر، وليس خطأً معزولًا. **الأخطر**:
      اكتُشف أن هذا الخلل **كسر إصلاح تباين كان قد نجح في المرحلة 3**
      (تحوّل من 4.55 صحيح إلى 3.45 فاشل فعليًا بسبب التسرب)، وأن قاعدة
      أخرى كانت ستُظهر **خلفية فاتحة كريمية خاطئة تمامًا داخل السمة
      الداكنة** (`.dashboard-task`). **الإصلاح**: سكريبت آلي دقيق أصلح
      كل الـ16 حالة دفعة واحدة (تحقُّق فوري: `pnpm run build` نجح،
      فحص آلي إعادي أكَّد صفر حالات متبقية). أُصلحت بعدها 4 أخطاء
      تباين حقيقية منفصلة (غير مرتبطة بخلل التسرب) في تذييل الصفحة
      ونافذة الكود. **النتيجة النهائية بعد كل الإصلاحات، مُتحقَّق منها
      بتشغيل Lighthouse فعلي ثانٍ**: **الوصولية 100/100** (من 88)،
      أفضل الممارسات 96، السيو 91، الأداء 66 (لم يتحسّن جوهريًا — أكبر
      فرصة تحسين متبقية هي ~410 كيلوبايت JavaScript غير مُستخدَم في
      حزمة sharedvendor الرئيسية، وهو أثر متوقَّع ومعروف لتطبيقات SPA
      بحزمة مشتركة، يحتاج إعادة هيكلة أعمق للحزم لم تُنفَّذ في هذه
      الجلسة نظرًا لحجمها ومخاطرها).
- [x] حذف `client/src/components/AIChatBox.tsx` (غير مستخدم إطلاقًا)
      وتبعية `framer-motion` (غير مستخدمة إطلاقًا — أُزيلت من
      `package.json`). **`recharts` أُبقيت عمدًا**: مُستخدَمة فعليًا من
      `components/ui/chart.tsx`، وهو أحد 37 مكوّن shadcn/ui أساسي غير
      مُستخدَم حاليًا لكنه جزء من مكتبة تصميم متاحة للاستخدام المستقبلي
      (نفس نمط باقي المكوّنات) — حذفه وحده كان قرارًا تعسفيًا وغير
      متسق، بخلاف `AIChatBox.tsx` الذي كان مكوّن ميزة كاملة يتيمة.

---

## ملخص الحالة الإجمالية

| الفئة | إلزامي مكتمل | إلزامي متبقٍّ |
| --- | --- | --- |
| الجودة الأساسية | 9 | 1 |
| الأمان | 15 | 0 |
| الدفع | 12 | 1 |
| المحتوى والشهادات | 18 | 0 |
| التوثيق | 3 | 0 |

**الخلاصة الصريحة:** المراحل 1-7 (الأساس، تجربة المتعلم، العربية
وإمكانية الوصول، جودة المحتوى والإدارة، الدفع والسوق الجزائرية، الأمان
والاعتمادية، الأداء والظهور) — وهي كل المراحل السبع المذكورة في
البرومبت الأصلي — مكتملة إلى حد كبير وموثَّقة بصدق، بما فيها إصلاح
ثغرتين أمنيتين حقيقيتين وخطيرتين (ملفات محمية بلا حماية إطلاقًا،
webhook واتساب بلا تحقق توقيع)، عدة أخطاء أداء/ظهور حقيقية (طلب شبكة
فاشل مضمون في كل تحميل، لا caching، لا meta tags، لا sitemap)، مراجعة
آلية شاملة (وليست عيّنات فقط) لملكية/صلاحيات 54 نقطة نهاية بأداة حقيقية
دائمة في المشروع (`scripts/audit-endpoint-ownership.ts`، صفر حالة
مشبوهة)، وتنظيف كامل لكل الادعاءات الوهمية عن SQLite في التوثيق.
**تحديث لاحق**: أُضيفت ميزة جديدة كاملة — **الدفع المباشر عبر الحساب
البريدي (CCP) برفع إيصال على الموقع**، بديل حقيقي لواتساب، بحماية
حقيقية من إعادة استخدام نفس صورة الإيصال (بصمة SHA-256 بقيد فريد على
مستوى قاعدة البيانات)، مُختبَرة بحالة E2E حقيقية جديدة ضد MySQL فعلية.
كذلك اكتُشف **تذبذب غير حتمي (flakiness) في اختبار واحد** عند التشغيل
المتوازي الكامل مع `DATABASE_URL` تحديدًا (تفصيله أعلاه في "الجودة
الأساسية") — غير متعلق بأي ميزة حديثة، ولا يؤثر على البوابات الأساسية
الموثوقة للمشروع (`pnpm test` بدون قاعدة بيانات، و`realDb.e2e.test.ts`
منفردًا)، لكنه يبقى بندًا مفتوحًا صريحًا.

**أربعة من خمسة أقسام إلزامية في هذا الجدول مكتملة بالكامل**:
الأمان (15/15)، المحتوى والشهادات (18/18)، والتوثيق (3/3)، والدفع
(باستثناء البند القانوني). **كل البنود "غير الإلزامية" أُغلقت أيضًا** — بما فيها
خلل RTL في الأسهم (23 موضعًا عبر 12 صفحة، مُتحقَّق منه بمتصفح حقيقي)،
تجاوز أفقي حرج على الهاتف، خلل حرج في تسجيل الدخول عبر HTTP، ومراجعة
Lighthouse حقيقية كاملة (الوصولية 100/100 بعد اكتشاف وإصلاح خلل CSS
بنيوي منهجي أثّر على 16 قاعدة عبر الملف بأكمله — انظر
`PHASE_VISUAL_AUDIT.md`). **البند الإلزامي الوحيد المتبقي في كامل هذا
الملف** هو مراجعة قانونية فعلية لسياسة الاسترجاع — يتطلب محاميًا
حقيقيًا، خارج نطاق أي عمل برمجي بطبيعته. **لا يزال لا يجوز إعلان
جاهزية السوق حتى استيفاء هذا البند الأخير**، إضافة لكون BaridiMob غير
جاهز فعليًا (قرار تجاري/تعاقدي، مُقرّ به بصدق في الكود نفسه) — كلاهما
خارج نطاق أي عمل برمجي. التفاصيل الكاملة لكل مرحلة موثَّقة في ملفات
`PHASE1_STATUS.md` حتى `PHASE7_STATUS.md`، `PHASE6_ENDPOINT_AUDIT.md`،
و`PHASE_VISUAL_AUDIT.md`.
