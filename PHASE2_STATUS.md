# حالة المرحلة 2 — جودة تجربة المتعلم

**تاريخ التحقق:** 2026-09-01 | **الحالة:** مكتملة، لم يُنتقل إلى المرحلة 3.

## منهجية العمل
فُحصت الصفحات الخمس المستهدفة صراحة في البرومبت (`Home.tsx`،
`CourseCatalog.tsx`، `CourseDetail.tsx`، `LessonViewer.tsx`،
`Dashboard.tsx`) سطرًا بسطر بحثًا عن فجوات حقيقية، إضافة إلى
`LearningFlows.tsx` و`AlgorithmLab.tsx` عند اكتشاف نمط مشابه. كل إصلاح
تحقق منه type-check، ثم أُعيد تشغيل كامل الاختبارات (197) واختبار E2E
الحقيقي (15) وproduction build بعد كل دفعة تعديلات.

## الأخطاء الحقيقية المُكتشَفة والمُصلَحة

### 1. لا توجد أي معالجة لحالة الخطأ في أي صفحة (فجوة نظامية)
لم تتحقق أي من الصفحات الخمس من `query.isError` — أي خطأ شبكة أو خادم
كان يُعامَل بصمت كـ"لا توجد بيانات"، وهو بالضبط ما يمنعه البرومبت
("عدم استخدام fallback صامت"). أُضيف نمط `.error-state` بصري متمايز
(حدود/لون تحذيري مختلف عن الحالة الفارغة) مع زر "إعادة المحاولة" حقيقي
(`refetch()`) في: `CourseCatalog.tsx`، `CourseDetail.tsx`،
`LessonViewer.tsx`.

### 2. روابط "استكشاف المسار" في الصفحة الرئيسية كانت معطوبة فعليًا
زرا "استكشف مسار الرياضيات/الإعلام الآلي" في الصفحة الرئيسية، وزرا "ابدأ
من الصفر" في `LearningFlows.tsx`، كانت تُوجّه إلى مسارات دورات **ثابتة
مُبَرمَجة** (`/courses/algebra-foundations`، `/courses/algorithms-zero`)
**غير موجودة في أي migration أو seed حقيقي** — أي أنها ستؤدي لصفحة "دورة
غير متاحة" على أي تنصيب حقيقي حتى يُنشئ أحد الإداريين دورة بنفس هذا
الاسم بالضبط بالصدفة. هذا يخالف مباشرة "التأكد من أن كل رابط وزر ينفذ
وظيفة حقيقية" و"استخدم بيانات حقيقية من قاعدة البيانات".
**الإصلاح:** تحويلها لروابط مبنية على المادة الحقيقية
(`/courses?subject=math`، `/courses?subject=computing`) — المادتان
موجودتان فعليًا دائمًا (مصدر بيانات حقيقي، `subjects` مُهيّأة في أول
migration). أُضيف دعم قراءة `?subject=` في `CourseCatalog.tsx` (لم يكن
موجودًا، الكتالوج كان يتجاهل query string بالكامل).
نفس العلة الأخف حدة وُجدت في `AlgorithmLab.tsx` (slug ثابت
`algorithms-zero` لتمرين غير مضمون الوجود) — أُصلحت بالرجوع لأول تمرين
حقيقي منشور في القائمة بدل قيمة ثابتة، مع الحفاظ على حالة "لا يوجد
تمرين" الصحيحة إن كانت القائمة فارغة فعلًا.

### 3. زر "استئناف التعلم" لا يستأنف من آخر درس فعليًا
كان الزر يوجّه دائمًا لصفحة الدورة العامة، وليس لآخر درس — يخالف
"تحسين استئناف التعلم من آخر درس" حرفيًا. السبب الجذري: لا يوجد أي حقل
في الخادم يحسب "آخر درس تفاعل معه المتعلم". **أُضيف حقل `resumeLessonId`
حقيقي** إلى `getLearnerSummary` (خادم): يحسب آخر درس بالنشاط الفعلي
(`lastActivityAt`) لكل دورة مسجَّلة، ويرجع لأول درس في ترتيب الدورة إن
لم يبدأ المتعلم بعد. غُطّي بحالتين في `realDb.e2e.test.ts` (قبل/بعد
نشاط حقيقي على الدرس) ضد قاعدة MySQL فعلية.

### 4. صفوف الدورات المسجَّلة في اللوحة كانت غير قابلة للنقر إطلاقًا
كانت `<div>` عادية مصممة بصريًا كبطاقات (حدود، خلفية، تخطيط) لكن بلا أي
`onClick` أو رابط — طريق مسدود بصريًا يوحي بإمكانية النقر. حُوّلت إلى
روابط `<Link>` حقيقية تؤدي لآخر درس (أو صفحة الدورة كحل احتياطي)، مع
إضافة `hover`/`focus-visible` واضحين في CSS.

### 5. عارض الدرس: تسمية زر معرَّفة لكن غير مستخدمة أبدًا
`goToCourse` ("الذهاب إلى صفحة الدورة") معرَّفة بثلاث لغات لكنها لم
تُستخدَم في أي مكان — حالات "غير مسجَّل"، "يتطلب اشتراكًا"، و"مقفل" في
`LessonViewer.tsx` كانت طرقًا مسدودة بلا أي إجراء ممكن سوى العودة
اليدوية. **الإصلاح يتطلب تعديلًا خلفيًا حقيقيًا**: أُضيف حقل `courseSlug`
إلى استجابة `getLessonForLearner` (لم يكن متاحًا للواجهة سابقًا في هذه
الحالات تحديدًا)، ثم فُعِّل الزر في الحالات الثلاث. غُطّي بحالة اختبار
حقيقية (`not_enrolled` + `courseSlug` صحيح) في `realDb.e2e.test.ts`.

### 6. تسمية "العودة إلى الدورة" في رأس صفحة الدرس كانت تشير لوجهة مختلفة
النص يقول "العودة إلى الدورة" لكن الرابط الفعلي كان دائمًا `/dashboard`
بلا علاقة بالدورة الحالية. صُحِّح النص الافتراضي ("لوحة التحكم")، مع
تمرير رابط ونص دقيقين (`/courses/{slug}` + "العودة إلى الدورة") في كل
موضع يتوفر فيه `courseSlug` فعليًا.

### 7. فشل الالتحاق بدورة مدفوعة كان يمر بصمت تمامًا
زر "التحق بالدورة" لم يكن يحتوي `onError` إطلاقًا — محاولة الالتحاق
بدورة مدفوعة دون اشتراك فعّال تفشل من الخادم (`FORBIDDEN`) لكن الزر
كان يعيد التفعيل دون أي رسالة توضح السبب. أُضيف `onError` يعرض toast
واضحًا يميّز بين "يتطلب اشتراكًا" (مع رابط مباشر لصفحة الأسعار) و"غير
متاح" وخطأ عام.

## تنظيف إضافي (اكتُشف أثناء العمل، خارج القائمة الأصلية لكن ذو صلة مباشرة)
تأكيد أن تعليق "SQLite mode" الوهمي (المُصحَّح في تقرير المرحلة 1 داخل
التوثيق فقط) كان مكررًا فعليًا داخل **17 ملف من كود المصدر الحقيقي**
(`server/db/*.ts`) — أُزيل من الكود بالكامل والتحقق أن type-check ما
زال ينجح بعدها.

## ما رُوجِع ولم يُغيَّر (قرار واعٍ، ليس إغفالًا)
- **تجربة الهاتف:** المشروع يحتوي فعليًا تغطية جيدة لعدة نقاط توقف
  (700px، 680px، 600px، 520px، 450px) عبر كل الصفحات المستهدفة؛ عناصر
  الفيديو والأزرار مرنة بالفعل. لم يُعثر على خلل وظيفي حقيقي يستحق تعديل
  CSS أعمى بلا معاينة بصرية فعلية على جهاز/محاكي حقيقي — **يُوصى
  بمراجعة بصرية يدوية على هاتف حقيقي قبل الإطلاق**، لأن هذا التحقق النصي
  لا يغني عنها بالكامل.
- **حالة خطأ `subjectsQuery` في الكتالوج:** تُركت متسامحة بصمت (`|| []`)
  لأنها بيانات تصفية ثانوية (تبويبات المواد) وليست المحتوى الرئيسي؛
  فشلها يعني فقط عدم ظهور تبويبات التصفية، لا تضليل المستخدم بأن الدورات
  غير موجودة.

## التحقق النهائي (نفس هذه الجلسة)
- `pnpm run check`: ✅ صفر أخطاء (بعد كل دفعة تعديلات).
- `pnpm test` بدون `DATABASE_URL`: ✅ 197 اختبارًا، 15 مُسقَطة بصدق.
- `DATABASE_URL=... pnpm test server/realDb.e2e.test.ts`: ✅ 15/15 (بما
  فيها التأكيدات الجديدة على `resumeLessonId` و`courseSlug`).
- `pnpm run build`: ✅ ناجح.

## الملفات المُعدَّلة في هذه المرحلة
`server/db/courses.ts` (resumeLessonId، courseSlug، 17 تعليق مُصحَّح)،
`server/db/subscriptions.ts`، `server/courseProgress.ts`،
`server/db/errorLog.ts`، `server/db/*.ts` (14 ملف، تعليق فقط)،
`server/realDb.e2e.test.ts` (اختبارات جديدة)،
`client/src/pages/{Home,CourseCatalog,CourseDetail,LessonViewer,Dashboard,LearningFlows,AlgorithmLab}.tsx`،
`client/src/index.css`، `PHASE2_STATUS.md` (هذا الملف).

لم يُحذف أو يُعَد تصميم أي شيء. الهوية البصرية والتقنيات الأساسية لم
تتغيّر. لم يُنتقل إلى المرحلة 3.
