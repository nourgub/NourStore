# حالة المرحلة 4 — جودة المحتوى والإدارة

**تاريخ التحقق:** 2026-09-01 | **الحالة:** مكتملة، لم يُنتقل إلى المرحلة 5.

## منهجية العمل
فحص كل بند من البرومبت مقابل الكود الفعلي (لا افتراضات)، تعديل بمخطط
migration حقيقي عند الحاجة، تطبيقه على قاعدة MySQL حقيقية من الصفر (20
migration بالترتيب)، وتحقق type-check/tests/build/E2E حقيقي بعد كل دفعة.

## الأخطاء الحقيقية المُكتشَفة والمُصلَحة

### 1. لا تحقق قبل نشر دورة (خطر إطلاق حقيقي)
`setCoursePublished` كانت تسمح بنشر دورة **بلا درس واحد** — تظهر للعامة
كدورة قابلة للنقر لكن فارغة تمامًا. أُضيف تحقق حقيقي: رفض النشر
(`BAD_REQUEST` بالإنجليزية بوضوح) إن لم يوجد درس واحد على الأقل، مع
معالجة `onError` في الواجهة تعرض toast واضحًا.

### 2. حذف محتوى بلا حماية من فقدان بيانات متعلمين حقيقيين
`deleteManagedCourse`/`Unit`/`Lesson` لم تكن تتحقق إطلاقًا من وجود
تسجيلات/تقدّم حقيقي قبل الحذف — كان يمكن أن يفشل بخطأ SQL خام غير مفهوم
(قيد foreign key) أو يفقد بيانات متعلمين. أُضيف فحص يرفض الحذف بوضوح
(`CONFLICT`) مع اقتراح "أوقف النشر بدلًا من الحذف"، ومعالجة toast مطابقة
في الواجهة.

### 3. تسجيل audit log غائب عن عمليات حساسة
حذف الدورة/الوحدة/الدرس، ونشر/إلغاء نشر الدورة: **لا شيء منها كان
يُسجَّل** في `adminAuditLog` رغم أن عمليات إدارية أخرى مشابهة تفعل ذلك.
أُضيف `logAdminAction` لكل عملية ناجحة من هذه الخمس.

### 4. تأكيد حذف غائب عن الدرس تحديدًا
حذف الدورة والوحدة يطلبان `window.confirm()`، لكن حذف الدرس كان **بلا
أي تأكيد** — نقرة واحدة تحذفه فورًا. أُصلح ليطابق النمط.

### 5. لا حالة "مؤرشف" — فقط ثنائية منشور/مسودة
أُضيف عمود `status` حقيقي (`draft`/`published`/`archived`) عبر migration
جديد (`0019`)، مع الحفاظ على `isPublished` كما هو لضمان توافق كل
الاستعلامات القديمة دون تعديلها (دورة مؤرشفة `isPublished=0` تمامًا مثل
المسودة — صحيحة تلقائيًا لكل الاستعلامات الحالية). أُضيف إجراء `archiveCourse`
حقيقي متمايز عن "إلغاء النشر"، مع زر واجهة وتأكيد.

### 6. لا preview قبل النشر — كان مستحيلًا فعليًا
`learning.course` (الموجّه العام) كانت تفلتر الدورات المنشورة فقط
**دون أي استثناء** — حتى المالك (المعلم) لم يكن قادرًا على معاينة دورته
الخاصة غير المنشورة. أُصلح لمنح صلاحية معاينة لمالك الدورة أو الإداري
تحديدًا، **محسوبة من هوية المستخدم في الخادم** (ليست معامل يرسله
العميل، تفاديًا لأي التفاف أمني). أُضيفت شارة "معاينة" واضحة بصريًا في
`CourseDetail.tsx`.

### 7. لا دعم فعلي لترتيب الوحدات/الدروس
اكتُشف أن **كل** وحدة/درس جديد كان يُنشأ بـ`orderIndex: 0` ثابتًا بلا أي
طريقة لاحقة لتغييره — يعني أن ترتيب العرض الفعلي كان عشوائيًا (يعتمد
على ترتيب الإرجاع من القاعدة، وليس على الحقل المخصص للترتيب). الإصلاح:
- إنشاء وحدة/درس جديد الآن يُلحَق تلقائيًا بنهاية القائمة الصحيحة.
- إجراءان جديدان `reorderUnit`/`reorderLesson` يُبدّلان `orderIndex` بين
  عنصرين متجاورين بشكل ذرّي وآمن (محصّنان ضد بيانات تاريخية بها تكرار
  في `orderIndex`، لأن التبديل يعتمد على الموضع في القائمة المرتَّبة
  وليس على القيمة نفسها).
- أزرار ↑/↓ فعلية في واجهة الإدارة لكل وحدة ودرس.

### 8. حقول الأهداف/المتطلبات/الفئة المستهدفة — غير موجودة إطلاقًا
لم يكن هناك أي عمود لهذا في المخطط. أُضيفت عبر نفس الـmigration:
`objectivesAr/Fr/En`، `prerequisitesAr/Fr/En` (قوائم JSON اختيارية)،
`targetAudienceAr/Fr/En` (نص اختياري). موصولة بالكامل في:
- `createCourse`/`updateManagedCourse` (الخادم، مع حماية من مسح القيم
  الموجودة عند تعديل لا يتضمنها — التحديث يتجاهل الحقل تمامًا إن لم
  يُرسَل، بدل مسحه إلى `null`).
- موجّهات `content.createCourse`/`content.updateCourse` (تحقق Zod).
- نموذج إنشاء دورة جديدة في لوحة الإدارة (حقول نصية/textarea حقيقية).
- عرض فعلي وشرطي في `CourseDetail.tsx` (لا يظهر أي قسم فارغ إن لم
  تُملأ الحقول — لا بيانات وهمية).

### 9. دورة تجريبية حقيقية مناسبة للسوق الجزائرية
`scripts/seed-demo-course-bem.ts` — سكريبت **منفصل تمامًا** عن أي كود
تطبيق أو migration، **idempotent** (تحقَّق فعليًا: التشغيل الثاني يكتشف
الدورة الموجودة ويخرج بأمان دون تكرار)، **غير منشور تلقائيًا** (يتطلب
مراجعة وموافقة إداري صريحة من لوحة التحكم). المحتوى:
- **رياضيات BEM حقيقية**: "المعادلات من الدرجة الأولى بمجهول واحد" —
  موضوع فعلي من منهاج السنة الرابعة متوسط الجزائري.
- 3 دروس بمحتوى رياضي **صحيح ومُتحقَّق يدويًا خطوة بخطوة** (تعريف، طريقة
  الحل بقاعدة النقل مع تغيير الإشارة، مسائل تطبيقية).
- اختبار وحدة (3 أسئلة) وامتحان نهائي (سؤالان) — **كل الإجابات الصحيحة
  تحققتُ من حسابها يدويًا** قبل إدراجها (مثال: 4x+1=13 → x=3 ✓).
- حقول الأهداف/المتطلبات/الفئة المستهدفة مملوءة بمحتوى حقيقي.
- **لا شهادات ولا آراء عملاء وهمية** — لم تُضَف أي بيانات من هذا النوع
  (التطبيق أصلًا لا يحتوي نظام مراجعات/testimonials).
- **تحقَّق التشغيل الفعلي**: شُغِّل ضد قاعدة MySQL حقيقية، البنية
  الناتجة (3 دروس بترتيب صحيح 0/1/2، اختبار وحدة بـ3 أسئلة، امتحان
  نهائي بسؤالين، إجابات مطابقة للمُتوقَّع) تحققتُ منها مباشرة عبر
  استعلامات SQL بعد التشغيل. النص العربي المُخزَّن سليم (تحقَّق عبر
  `utf8mb4`).

## ما لم يُنجَز — مؤجَّل صراحة

### تعديل الأهداف/المتطلبات بعد الإنشاء
نموذج "تعديل الدورة" الحالي (موجود مسبقًا، ليس من إنشائي) يعتمد على
سلسلة `window.prompt()` متتالية للعنوان والوصف فقط. إضافة 9 نوافذ
`prompt()` أخرى (3 لغات × 3 حقول) لتعديل الأهداف/المتطلبات/الفئة
المستهدفة كانت ستنتج تجربة مستخدم غير مقبولة (12 نافذة متتالية). لم
أُنفّذ هذا — **الحقول قابلة للتعبئة عند الإنشاء فقط حاليًا**؛ تعديلها
لاحقًا يحتاج نموذج/نافذة تعديل حقيقية بدل سلسلة prompt(). موصى به
كبند منفصل (تحسين واجهة الإدارة، خارج نطاق migration/منطق الخادم الذي
أُنجز بالكامل).

### خلل موجود مسبقًا في سكريبت الترحيل (اكتُشف هنا، أُصلح لاحقًا)
`scripts/migrate.mjs` كان يفشل عند إعادة تشغيله على قاعدة مُرحَّلة جزئيًا
بسبب كشف "مُطبَّق مسبقًا" غير دقيق لبعض رسائل أخطاء SQL (تحديدًا
`DROP INDEX` على فهرس محذوف مسبقًا في migration 0017). **تم إصلاحه
لاحقًا** (بعد إتمام المراحل السبع) بجدول تتبع حقيقي (`_migrations`) بدل
تخمين رسائل الأخطاء، ومُختبَر فعليًا بأربعة سيناريوهات ضد MySQL/MariaDB
— انظر `PRELAUNCH_CHECKLIST.md` والتعليقات في `scripts/migrate.mjs`
نفسه للتفاصيل الكاملة.

## التحقق النهائي (نفس هذه الجلسة)
- `pnpm run check`: ✅ صفر أخطاء (بعد كل دفعة تعديلات).
- `pnpm test` بدون `DATABASE_URL`: ✅ 197 اختبارًا، 15 مُسقَطة بصدق.
- `DATABASE_URL=... pnpm test server/realDb.e2e.test.ts`: ✅ 15/15.
- `pnpm run build`: ✅ ناجح.
- `node scripts/migrate.mjs` من الصفر: ✅ 20 migration بالترتيب.
- `npx tsx scripts/seed-demo-course-bem.ts`: ✅ نجح من أول تشغيل، وأثبت
  idempotency فعليًا (تشغيل ثانٍ لم يُكرِّر شيئًا)، والبيانات المُخزَّنة
  تحققت صحتها هيكليًا ولغويًا مباشرة عبر SQL.

## الملفات المُعدَّلة/المُضافة في هذه المرحلة
`drizzle/schema.ts`، `drizzle/0019_add_content_quality_fields.sql`
(جديد)، `server/db/courses.ts` (تحقق النشر، حماية الحذف، الأرشفة،
المعاينة، إعادة الترتيب، الحقول الجديدة)، `server/routers.ts` (موجّهات
مطابقة + audit log)، `client/src/pages/LearningFlows.tsx` (واجهة
الإدارة: حالة ثلاثية، أرشفة، معاينة، ترتيب، حقول جديدة، تأكيد حذف
الدرس، معالجة أخطاء)، `client/src/pages/CourseDetail.tsx` (شارة معاينة
+ عرض الحقول الجديدة)، `scripts/seed-demo-course-bem.ts` (جديد)،
`PHASE4_STATUS.md` (هذا الملف).

لم يُحذف أو يُعَد تصميم أي شيء. لم يُنتقل إلى المرحلة 5.
