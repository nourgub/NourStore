# تقرير تنفيذ برومبت NouriX Academy

> **تصحيح (2026-09-01):** هذا تقرير تاريخي من مرحلة تنفيذ مبكرة جدًا في
> عمر المشروع. أرقامه ووصفه **متجاوَزان بالكامل الآن**: الرقم "34
> اختبارًا" أدناه أصبح غير صحيح (العدد الفعلي الحالي 197+)، ويصف قسم
> "ما بقي خارج هذه الدفعة" غياب ميزات (Gamification، الشهادات، دفع
> واتساب) **أُضيفت جميعها لاحقًا وموجودة فعليًا في الكود الآن** (انظر
> `server/db/gamification.ts`، `server/db/certificates.ts`،
> `server/db/whatsappPayments.ts`). كذلك فإن `server/db.ts` المذكور هنا
> كملف واحد أصبح الآن ملف barrel يُعيد التصدير من `server/db/*.ts`
> (وحدات منفصلة حسب النطاق). اعتبر هذا الملف سجلًا تاريخيًا فقط؛ الحالة
> الحالية والمُتحقَّق منها موجودة في `PHASE1_STATUS.md` بجذر المشروع.

## نطاق التنفيذ

تم تطوير المشروع القائم دون إعادة بنائه من الصفر ودون إزالة البنية الحالية. حافظت التعديلات على React/Vite وExpress وtRPC وDrizzle وManus OAuth، وأضافت طبقات وظيفية مرتبطة بالـ API وقاعدة البيانات بدل الاكتفاء بواجهات شكلية.

## ما تم إصلاحه وتنفيذه

| المجال | النتيجة |
| --- | --- |
| عقدة WhatsApp العامة | أصبحت تعيد قيمة نصية أو `null` فقط، بما يمنع تسريب صف قاعدة البيانات أو قيمة غير متوقعة إلى الواجهة. |
| البحث الموحد | تمت إضافة `learning.search` مع بحث في الدورات والدروس والتمارين المنشورة، وتصفية المادة والمستوى وحدّ للنتائج، إضافة إلى صفحة `/search`. |
| عدم استخدام البيانات الوهمية | أزيلت fixtures الثابتة من كتالوج الدورات وتفاصيل الدورة؛ أصبحت الشاشات تعتمد على البيانات المنشورة من الخادم وتعرض حالة فارغة عند عدم وجودها. |
| الشهادات | تمت إضافة جدول `certificates`، والتحقق العام عبر `certificates.verify`، وصفحة `/verify/certificate/:id`. تُصدر الشهادة تلقائيًا عند بلوغ إكمال الدورة 100%، مع منع إصدارها قبل اكتمال التسجيل. |
| محرك الإكمال | أصبحت عملية إكمال الدرس تحفظ التقدم أو تحدّثه، تحسب نسبة إكمال الدورة، تحدّث حالة التسجيل، وتمنع التكرارات المنطقية في سجل تقدم الدرس. |
| مركز الإشعارات | تمت إضافة جدول `notifications` وإجرائي القراءة وتحديد الإشعار كمقروء مع صفحة `/notifications` محمية بالمصادقة. لا يمكن للمستخدم تعديل إشعار يخص مستخدمًا آخر. |
| التعدد الجغرافي | أضيفت للمستخدم حقول الدولة والعملة واللغة والمنطقة الزمنية. وأضيفت لخطة الاشتراك نوع الخطة والعملة، مع إبقاء `priceCents` كقيمة صحيحة صغيرة قابلة للربط بعملة الخطة. |
| جاهزية مزود الدفع | أضيفت حقول provider-neutral للاشتراكات (`paymentProvider`, `providerCustomerId`, `providerSubscriptionId`) مع الإبقاء على الحقل القديم للتوافق الانتقالي. لم تتم إضافة بوابة دفع وهمية أو تخزين بيانات بطاقات. |
| حماية ملكية المحتوى | أصبحت إنشاءات الوحدات والدروس تتحقق من ملكية الدورة أو الوحدة على الخادم، إضافة إلى التحققات الموجودة مسبقًا في التعديل والحذف والأسئلة والملفات. |
| الوصول من لوحة الطالب | أضيفت روابط الشهادات والإشعارات إلى لوحة الطالب. |
| التصميم المتجاوب | أضيفت قواعد responsive وحالة `prefers-reduced-motion` للشاشات الجديدة مع الحفاظ على نظام الألوان الحالي. |

## ملفات رئيسية تم تعديلها

تم تعديل `drizzle/schema.ts` و`server/db.ts` و`server/routers.ts` و`client/src/App.tsx` و`client/src/pages/CourseCatalog.tsx` و`client/src/pages/CourseDetail.tsx` و`client/src/pages/Dashboard.tsx` و`client/src/index.css`. كما أضيفت الصفحات `Search.tsx` و`CertificateVerify.tsx` و`Notifications.tsx`، واختبارات عقد عامة إضافية، وملف الترحيل `drizzle/0009_lms_hardening.sql`.

## التحقق

| الأمر | النتيجة |
| --- | --- |
| `pnpm check` | ناجح دون أخطاء TypeScript. |
| `pnpm test` | ناجح: 5 ملفات اختبار و34 اختبارًا. |
| `pnpm build` | ناجح للواجهة والخادم. |
| `git diff --check` | لا توجد مخرجات في النسخة التي تم فحصها؛ الأرشيف الأصلي ليس مستودع Git كاملًا. |

ظهر أثناء البناء تحذيران موجودان في القالب حول متغيري Umami الاختياريين، وتحذير حجم bundle يتجاوز 500 kB. لا يمنع أي منهما البناء، لكن يُستحسن ضبط متغيرات التحليلات وتقسيم الحزمة قبل الإطلاق التجاري.

## الترحيل وقبل الإطلاق

تعذر تشغيل مولّد Drizzle تلقائيًا لأن بيئة التنفيذ الحالية لا تحتوي على `DATABASE_URL`. لذلك أُضيف ملف SQL واضح باسم `drizzle/0009_lms_hardening.sql` ليُطبّق على قاعدة البيانات بعد أخذ نسخة احتياطية، ثم يجب تشغيل اختبارات API ضد قاعدة بيانات اختبارية فعلية. لا ينبغي اعتبار الدفع جاهزًا للإنتاج قبل ربط مزود دفع حقيقي واختبار webhooks والتوقيعات idempotency، كما لا ينبغي اعتبار الإشعارات خارج التطبيق جاهزة قبل ربط مزود بريد أو Push حقيقي.

## ما بقي خارج هذه الدفعة

لم تتم إضافة AI أو Automation أو WhatsApp Automation، التزامًا بالبرومبت. كما أن محرك الامتحانات الكامل، نظام Skills المتدرج، التحليلات الزمنية، Gamification، onboarding متعدد الأدوار، الفواتير والضرائب، وخدمات الدفع المحلية والدولية تحتاج إلى دفعات مستقلة لأنها تتطلب نماذج قاعدة بيانات واختبارات تكامل وقرارات تشغيلية إضافية. البنية الحالية أصبحت قابلة لإضافتها لاحقًا دون إدخال AI أو Automation إلى نواة LMS الحالية.
