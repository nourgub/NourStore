# حالة المرحلة 6 — الأمان والاعتمادية

**تاريخ التحقق:** 2026-09-01 | **الحالة:** مكتملة جزئيًا بصدق (نطاق واسع
جدًا، بعض البنود مؤجَّلة صراحة أدناه).

## منهجية العمل
فحص كل بند مقابل الكود الفعلي، مع تمييز واضح بين "سليم مسبقًا" (تحقُّق
دون تعديل) و"فجوة حقيقية" (إصلاح + اختبار). كل إصلاح تحقق منه type-check
ثم إعادة تشغيل كامل الاختبارات (204) واختبار E2E الحقيقي (17) وproduction
build.

## اكتشافات أمنية حقيقية وخطيرة — أُصلحت

### 1. روابط الملفات المحلية كانت بلا أي حماية إطلاقًا (الأخطر)
`/local-storage/*` كان مسارًا ثابتًا بلا مصادقة أو تحقق ملكية على
الإطلاق — يشمل **صور إيصالات الدفع** (بيانات مالية حساسة فعلية) و
**فيديوهات/مرفقات الدروس المدفوعة**. طبقة tRPC كانت تُقيّد مَن يُخبَر
بالرابط (الإداري لصور الإيصالات، المتعلم المسجَّل للمرفقات)، لكن الرابط
نفسه بعد ذلك يعمل للأبد لأي شخص يعرفه (تسريب لقطة شاشة، تاريخ متصفح،
جهاز مخترق).
**الإصلاح**: `server/protectedFiles.ts` — نقطتا نهاية تُعيدان التحقق من
الصلاحية **في كل طلب**:
- `/api/protected-files/receipt/:receiptId` — إداري أو صاحب الفاتورة فقط.
- `/api/protected-files/lesson-asset/:assetId` — يُعيد استخدام منطق
  صلاحية الدرس الفعلي (تسجيل + عدم قفل).
كلاهما محميان بنفس حارس path traversal، ويُطبَّقان فقط عند
`STORAGE_PROVIDER=local` (وضع S3 محمي أصلًا بروابط AWS موقّعة حقيقية
منتهية الصلاحية — تحقَّقت من ذلك في الكود، لا حاجة لتعديله).

### 2. webhook واتساب (فعلي وعامل) بلا تحقق توقيع إطلاقًا
`server/whatsappWebhook.ts` كان يقبل أي POST دون أي تحقق من مصدره — أي
شخص يكتشف رابط الـwebhook يمكنه تزوير رسائل واردة (مثلاً "إيصال دفع"
وهمي من رقم عشوائي). **الإصلاح**: تحقق HMAC-SHA256 حقيقي مطابق
لمواصفات Meta الموثَّقة (`X-Hub-Signature-256`)، مع التقاط الجسم الخام
الحقيقي (`req.rawBody`) عبر تعديل `express.json()` — ضروري لأن التوقيع
يُحسَب على البايتات الخام بالضبط، وليس على إعادة تسلسل JSON. يفشل
بأمان (501) إن لم يُضبَط `WHATSAPP_APP_SECRET`، بدل قبول أي شيء بصمت.
**مُختبَر فعليًا** بـ7 اختبارات حقيقية (توقيع صحيح/مزوَّر/محتوى مُعدَّل/
رأس مفقود/تنسيق خاطئ).

### 3. نفس القصور المُوثَّق ذاتيًا في webhook الدفعات — أُصلح فعليًا
كان تعليق الكود نفسه يُقرّ بأن التحقق يستخدم `JSON.stringify(req.body)`
بدل البايتات الخام الحقيقية (غير كافٍ لمزوّدين حقيقيين مثل Stripe).
أُصلح باستخدام `req.rawBody` نفسه المُضاف في الإصلاح أعلاه.

## اكتشافات حقيقية أخرى — أُصلحت

### 4. سجل الأخطاء لم يكن قابلاً للبحث فعليًا
لوحة "سجل الأخطاء" الإدارية كانت تدعم فقط تصفية بحالة الحل (محلولة/غير
محلولة) — **لا بحث نصي إطلاقًا** رغم أن البرومبت يطلب صراحة "سجل أخطاء
قابل للبحث". أُضيف بحث حقيقي (`LIKE` على الرسالة/السياق/الـstack) في
الخادم والواجهة.

### 5. لا نقطة نهاية عامة محمية بـrate limiting
`certificates.verify` (عامة، بلا مصادقة) كانت بلا أي حد للطلبات. ليست
قابلة لهجوم "brute-force" حقيقي (معرِّفات الشهادات `nanoid(12)` عشوائية
تمامًا)، لكنها كانت بلا حماية إطلاقًا من الاستنزاف. أُضيف حد معقول
(60/ساعة لكل IP).

### 6. نسخ احتياطي/استرجاع لم يكونا موجودين إطلاقًا
لا توجد أي وثيقة أو سكريبت للنسخ الاحتياطي. أُضيف
`scripts/backup-database.sh` و`scripts/restore-database.sh` — سكريبتات
حقيقية تعمل فعليًا (ليست placeholders)، متوافقة مع إعداد docker-compose
الموجود، بتأكيد تفاعلي إلزامي قبل أي استرجاع (لا `--force`، مقصودة
لمنع كارثة استرجاع خاطئ). **تحقَّقت فعليًا من دورة نسخ احتياطي/استرجاع
كاملة** ضد MySQL/MariaDB حقيقية: 42 جدولًا استُرجِعت مطابقة تمامًا
للأصل بعد النسخ والاسترجاع. وثِّق الاستخدام في `DEPLOYMENT.md` مع مثال
جدولة cron حقيقي.

### 7. اكتُشف قسم SQLite وهمي آخر لم يُصلَح في المرحلة 1
قسم كامل في `DEPLOYMENT.md` ("Base de données zéro-service-externe :
SQLite intégré") كان يصف نفس الميزة الوهمية المُصحَّحة في `AUDIT.md`
خلال المرحلة 1 — بما فيها ادعاء "ثغرة أمنية حقيقية اكتُشفت وأُصلحت" غير
موجودة إطلاقًا. **نُسي إصلاح هذا القسم تحديدًا حينها.** حُذف الآن.
كذلك صُحِّح ادعاء آخر في نفس الملف يقول إن `scripts/migrate.mjs` "يكتشف
الترحيلات المُطبَّقة مسبقًا وينجح دائمًا عند إعادة التشغيل" — وهذا
يناقض ما اكتُشف فعليًا في المرحلة 4 (يفشل أحيانًا). صُحِّح ليعكس الواقع.

## ما رُوجِع ووُجد سليمًا بعمق (لم يحتج تعديلًا)
- `passwordHash`: لا يظهر خارج `usersAuth.ts` في كامل المشروع (بحث شامل).
- `answerKey`: كل استعلام يُرسل أسئلة اختبار للمتعلم يستثنيه صراحةً عبر
  `select()` محدَّد الأعمدة — مبني بشكل صحيح من البداية.
- التحقق من رفع الملفات: عميق فعليًا مسبقًا (تحقق امتداد/MIME متبادل،
  حظر امتدادات تنفيذية حتى المُخفاة كامتداد مزدوج، تحقق حجم البايتات
  الفعلي مقابل المُصرَّح به، منع path traversal في اسم الملف). القصور
  الوحيد المتبقي: لا تحقق من "البصمة السحرية" الحقيقية لمحتوى الملف
  (magic bytes) — يعتمد على تطابق الامتداد مع MIME المُصرَّح به فقط،
  وليس على فحص البايتات الأولى الفعلية. خطورة منخفضة (الامتدادات
  التنفيذية محظورة بالفعل بغض النظر)، مؤجَّل.
- rate limiting: تغطية جيدة مسبقًا (تسجيل، دخول، دفع، اختبارات، دعوات
  الأولياء، رفع ملفات، تذاكر الدعم).
- idempotency الشهادات: قيد فريد حقيقي على مستوى قاعدة البيانات
  (`UNIQUE(userId, courseId)`) — حماية مزدوجة حقيقية ضد أي سباق تزامن.
- idempotency الإشعارات: منطق de-dup حقيقي ومدروس (يقارن وقت آخر إشعار
  بوقت آخر تحديث للحالة المعنية) لكل من تذكيرات الاشتراك والإيصالات.
- عدم تسجيل بيانات حساسة: لا كلمات مرور/رموز في أي `console.log` (بحث
  شامل)، وسجل الأخطاء لا يلتقط جسم الطلب الكامل أصلًا.

## ما لم يُنجَز — مؤجَّل صراحة (نطاق ضخم جدًا لجلسة واحدة)
- **مراجعة شاملة لكل endpoint (118+) للمصادقة/الصلاحيات/الملكية**: فُحصت
  عيّنات مركَّزة ذات خطورة عالية فقط (الملفات، الـwebhooks، كلمات
  المرور/answerKey). المراجعة الشاملة الكاملة لكل نقطة نهاية بشكل فردي
  خارج نطاق الوقت المتاح — يحتاج جلسة مخصصة منهجية (قائمة تحقق لكل
  endpoint على حدة).
- **magic-byte verification** لمحتوى الملفات المرفوعة (انظر أعلاه).
- **مراجعة شاملة لتسجيل بيانات حساسة في لوحات الإدارة تحديدًا** — ركّز
  الفحص على `console.log`/`logError`، لم تُراجَع كل نقاط عرض البيانات
  في `LearningFlows.tsx` (ملف ضخم جدًا) بنفس العمق.
- **اختبار حقيقي لنقطتي `protectedFiles.ts`** ضد خادم HTTP فعلي (Express)
  — تحققتُ من المنطق الأمني عبر قراءة الكود بعناية، لكن لم أكتب اختبار
  HTTP فعلي (يحتاج supertest أو ما شابه، لم يُضَف للمشروع).

## التحقق النهائي (نفس هذه الجلسة)
- `pnpm run check`: ✅ صفر أخطاء (بعد كل دفعة تعديلات).
- `pnpm test` بدون `DATABASE_URL`: ✅ 204 اختبارًا (زادت من 197 — 7
  اختبارات جديدة لتوقيع webhook واتساب)، 17 مُسقَطة بصدق.
- `DATABASE_URL=... pnpm test server/realDb.e2e.test.ts`: ✅ 17/17.
- `pnpm run build`: ✅ ناجح.
- **دورة نسخ احتياطي/استرجاع حقيقية**: ✅ مُختبَرة يدويًا ضد
  MySQL/MariaDB حقيقية (42 جدولًا مطابقة بعد الاسترجاع).

## الملفات المُعدَّلة/المُضافة في هذه المرحلة
`server/protectedFiles.ts` (جديد)، `server/whatsappWebhook.ts` +
`server/whatsappWebhook.test.ts` (جديد)، `server/paymentsWebhook.ts`،
`server/_core/index.ts` (rawBody + تسجيل المسارات الجديدة)،
`server/_core/env.ts` (`WHATSAPP_APP_SECRET`)، `server/db/courses.ts`
(روابط مرفقات محمية)، `server/db/whatsappPayments.ts` (روابط إيصالات
محمية)، `server/db/errorLog.ts` + `server/routers.ts` (بحث سجل
الأخطاء)، `client/src/pages/LearningFlows.tsx` (مربع بحث سجل الأخطاء)،
`scripts/backup-database.sh` (جديد)، `scripts/restore-database.sh`
(جديد)، `DEPLOYMENT.md` (توثيق النسخ الاحتياطي + تصحيحات SQLite/
migrate.mjs إضافية)، `PHASE6_STATUS.md` (هذا الملف).

لم يُحذف أو يُعَد تصميم أي شيء. لم يُنتقل إلى المرحلة 7.
