# مراجعة بصرية فعلية بمتصفح حقيقي — تكملة المرحلة 3

**تاريخ التحقق:** 2026-09-01 | **الحالة:** اكتشاف منهجي مهم غيّر طبيعة
التحقق من عدة بنود كانت مُصنَّفة "تحتاج أداة غير متوفرة".

## الاكتشاف
بعد جلسات متعددة اعتمدت على الفحص النصي والمنطقي فقط لبنود RTL/الترجمة
(بسبب افتراض عدم وجود أداة معاينة بصرية حقيقية للتطبيق)، اكتُشف أن
البيئة تحتوي فعليًا على:
- ثنائي **Chrome for Testing حقيقي** قابل للتنفيذ
  (`~/.cache/puppeteer/chrome/linux-131.0.6778.204/chrome-linux64/chrome`).
- حزمة **Playwright** مُثبَّتة عالميًا وقابلة للاستخدام معه.

هذا يعني أن بنودًا وُصفت سابقًا بصراحة كـ"مؤجَّلة لغياب أداة معاينة
بصرية" **كانت في الواقع قابلة للتحقق طوال الوقت** — لم تُكتشَف الأداة
فقط. هذا درس مهم: التحقق من توفر الأدوات فعليًا (بدل الافتراض) وفّر
قيمة حقيقية هنا.

## المنهجية
1. بناء التطبيق (`pnpm run build`) وتشغيله فعليًا (`node dist/index.js`)
   مع قاعدة MySQL حقيقية وبيانات مزروعة حقيقية (دورة BEM منشورة بوحدة
   ودرسين).
2. تشغيل Playwright (مُوجَّه لثنائي Chrome الحقيقي) للتنقل الفعلي بين
   الصفحات وأخذ لقطات شاشة حقيقية (PNG) — بالعربية والفرنسية.
3. فحص كل لقطة بصريًا (أداة `view`) قبل أي استنتاج.
4. إصلاح ما تأكَّد بصريًا أنه خطأ فعلي، ثم إعادة الالتقاط للتحقق من
   الإصلاح فعليًا (وليس افتراض نجاحه).
5. تنظيف بيانات الاختبار المؤقتة وإيقاف الخادم بعد الانتهاء.

## الاكتشافات والإصلاحات المُتحقَّق منها بصريًا

### 1. خلل اتجاه الأسهم — مؤكَّد بصريًا وأُصلح جزئيًا
اللقطة الأولى (فرنسية) أظهرت زر "Explorer les parcours" بسهم يشير
**لليسار** رغم أن الاتجاه المنطقي للأمام في LTR هو اليمين — تأكيد بصري
قاطع لخلل كان موثَّقًا نظريًا منذ المرحلة 3 دون تحقق فعلي.

**الإصلاح**: مكوّن مركزي جديد `client/src/components/DirectionalArrow.tsx`
(`ForwardArrow`/`BackArrow`) يختار الأيقونة الصحيحة تلقائيًا حسب
الاتجاه. طُبِّق بعد تصنيف دلالي دقيق لكل سهم (هل يمثّل حركة للأمام أم
للخلف؟) على:
- `Home.tsx`: 5 مواضع (كلها "أمام": استكشاف، بدء الاختبار، إلخ).
- `CourseCatalog.tsx`: موضع واحد (اكتُشف أن `dir` لم يكن مُعرَّفًا
  كمتغيّر في هذا الملف تحديدًا — أُضيف).
- `CourseDetail.tsx`: 3 مواضع (موضعان "أمام"، وموضع "رجوع" واحد).
- `LessonViewer.tsx`: 4 مواضع (بما فيها منطق "السابق"/"التالي" الأهم
  دلاليًا في التطبيق).

**التحقق البعدي**: أُعيد التقاط لقطات الصفحة الرئيسية والكتالوج وتفاصيل
الدورة بالفرنسية — **الأسهم "الأمامية" تشير الآن لليمين بشكل صحيح**،
وزر "رجوع" (`← Retour aux parcours`) بقي يشير لليسار بشكل صحيح (لم
يتأثر سلبًا).

**نطاق الإصلاح — صريح وجزئي**: صفحات أخرى لم تُفحَص/تُصلَح بعد بنفس
الطريقة (`AlgorithmLab.tsx` وغيرها) — يحتاج جلسة متابعة.

### 2. اكتشاف جديد تمامًا: نص إنجليزي ثابت في نسخة فرنسية
لم يكن هذا معروفًا من أي فحص نصي سابق. اللقطة البصرية لصفحة تفاصيل
الدورة بالفرنسية أظهرت قسم "Pourquoi ce parcours ?" يعرض ثلاث جمل
**إنجليزية بالكامل** ("Progress from the fundamentals"، إلخ) رغم أن
بقية الصفحة فرنسية تمامًا. فحص الكود كشف السبب: الشرط كان
`lang === "ar" ? "عربي" : "إنجليزي"` — يتحقق من العربية فقط ثم يسقط
للإنجليزية مباشرة، متجاوزًا الفرنسية بالكامل.

**الإصلاح**: أُضيفت ترجمة فرنسية حقيقية للجمل الثلاث. **التحقق البعدي**:
أُعيد التقاط اللقطة — القسم يعرض الآن فرنسية صحيحة بالكامل.

### 3. فحص آلي إضافي لنفس نمط الخلل
بحثًا عن حالات مماثلة (تعبير ثنائي عربي/إنجليزي بلا فرع فرنسي) عبر كل
الواجهة: **240 نتيجة محتملة**. أغلبها إيجابيات كاذبة (فحوصات `dir` أو
حقول نماذج الإدارة متعددة اللغات المقصودة عمدًا — راجع `PHASE3_STATUS.md`
لتفصيل هذا التمييز). رُوجعت العيّنات العامة (غير الإدارية) يدويًا،
وأُصلحت أخطاء حقيقية إضافية:
- `Support.tsx`: تنسيق تاريخ كان يستخدم `fr-FR` كـfallback للإنجليزية
  أيضًا (بمعنى أن مستخدم الإنجليزية كان يرى تواريخ بتنسيق فرنسي)، ونص
  "اختر تذكرة لعرض المحادثة" بلا فرع فرنسي.
- `Pricing.tsx`: نص تأكيد الكوبون، ووحدة قياس مدة الخطة ("d" بدل "j"
  للفرنسية).
- `AlgorithmLab.tsx`: نص "جاري التحميل" بلا فرع فرنسي.
- `LearningFlows.tsx`: **5 حالات** من نفس نمط "جاري التحميل" (نصوص
  واجهة عامة، وليست حقول لغة مقصودة).

## التحقق النهائي (نفس هذه الجلسة)
- `pnpm run check`: ✅ صفر أخطاء (بعد كل دفعة تعديلات).
- `pnpm test` بدون `DATABASE_URL`: ✅ 216 اختبارًا، 18 مُسقَطة بصدق.
- `pnpm test` **مع** `DATABASE_URL` حقيقية: ✅ 231 نجح، 3 أُسقطت بوضوح،
  صفر فشل.
- `DATABASE_URL=... pnpm test server/realDb.e2e.test.ts`: ✅ 18/18.
- `pnpm run build`: ✅ ناجح.
- **لقطات شاشة حقيقية قبل/بعد لكل إصلاح** — محفوظة مؤقتًا أثناء الجلسة
  وحُذفت بعد التحقق (لم تُترَك في المشروع).

## متابعة (نفس اليوم، جلسة لاحقة): توسيع الفحص + اكتشاف حرج على الهاتف

### توسيع إصلاح الأسهم لـ8 صفحات إضافية
بنفس المنهجية (تصنيف دلالي لكل سهم، تحقق type-check فوري): أُصلحت
`AlgorithmLab.tsx`، `Auth.tsx`، `CertificateVerify.tsx`، `Dashboard.tsx`
(3 مواضع)، `Legal.tsx`، `Pricing.tsx`، `Search.tsx` (موضعان)،
`Support.tsx` (موضعان) — **13 موضعًا إضافيًا**، بإجمالي **23 موضعًا عبر
12 صفحة**. في 3 ملفات (`Auth.tsx`، `CertificateVerify.tsx`، `Search.tsx`)
لم يكن متغيّر `dir` مُعرَّفًا إطلاقًا، أُضيف بنفس النمط المُستخدَم في
باقي الملفات.

### اكتشاف حرج: أول معاينة حقيقية بحجم هاتف كشفت تجاوزًا أفقيًا فعليًا
حتى هذه اللحظة، كل اللقطات كانت بحجم سطح مكتب (1280×900). عند التقاط
أول لقطة بحجم هاتف حقيقي (390×844، مطابق لـiPhone 12/13) لصفحة كتالوج
الدورات، ظهر تجاوز أفقي واضح — مبدّل اللغة (EN/FR/AR) مقطوع جزئيًا عند
الحافة اليسرى للشاشة.

**التشخيص**: تشغيل استعلام `getBoundingClientRect()` فعلي على كل عنصر
في DOM المُصيَّر (عبر `page.evaluate()` في Playwright) حدَّد بدقة رياضية
أن `.catalog-header-actions`/`.catalog-lang` كانا يمتدان إلى
`left: -51px` — أي أن رأس الصفحة (شعار + رابط "الرئيسية" + رابط
"المختبر" + أيقونة بحث + مبدّل سمة + مبدّل لغة ثلاثي) أوسع فعليًا من
عرض الشاشة (441px منطقي داخل 390px متاح).

**الإصلاح**:
1. توسيع قاعدة CSS الموجودة (كانت تُخفي رابط "الرئيسية" النصي فقط عند
   ≤600px) لتُخفي رابط "المختبر" النصي أيضًا.
2. إضافة `overflow-x: hidden` على `html`/`body` كشبكة أمان دفاعية عامة
   — لا يُصلح مصدر أي تجاوز مستقبلي، لكنه يمنع تحوّله لتمرير أفقي مُخِلّ
   بتجربة المستخدم في حال وجود حالات مشابهة غير مُكتشَفة بعد في صفحات
   أخرى لم تُفحَص بحجم هاتف بعد.

**التحقق البعدي — رقمي وبصري معًا**:
- `document.documentElement.scrollWidth === document.documentElement.clientWidth`
  بالضبط (390 = 390) بعد الإصلاح، مقابل (441 ≠ 390) قبله — دليل رقمي
  قاطع، وليس انطباعًا بصريًا فقط.
- لقطة شاشة جديدة تؤكد أن الرأس بالكامل (بما فيه EN/FR/AR) محصور الآن
  داخل حدود الشاشة.

### التحقق النهائي بعد هذه الدفعة
- `pnpm run check`: ✅ صفر أخطاء.
- `pnpm test` بدون `DATABASE_URL`: ✅ 216 اختبارًا، 18 مُسقَطة بصدق.
- `pnpm test` **مع** `DATABASE_URL` حقيقية: ✅ 231 نجح، 3 أُسقطت، صفر فشل.
- `DATABASE_URL=... pnpm test server/realDb.e2e.test.ts`: ✅ 18/18.
- `pnpm run build`: ✅ ناجح.

## متابعة ثانية (نفس اليوم): اكتشاف حرج أثناء محاولة الوصول الفعلي لعارض الدرس

### السياق
عند محاولة تسجيل دخول حقيقي (عبر النموذج الفعلي، بريد إلكتروني وكلمة
مرور حقيقيين لمستخدم مزروع في قاعدة بيانات حقيقية) للوصول لعارض الدرس
واختبار الوحدة على حجم هاتف، **فشل تسجيل الدخول مرارًا** رغم أن بيانات
الاعتماد صحيحة.

### التشخيص
تتبُّع استجابات الشبكة الفعلية (`page.on("response")` في Playwright)
كشف أن الطلب `loginWithEmail` يُرجع **200 ونجاحًا حقيقيًا**
(`{"result":{"data":{"json":{"ok":true}}}}`) — لكن التنقل للصفحة
التالية يُظهر حالة "غير مسجَّل دخول" دائمًا. هذا يعني أن الخادم يقبل
بيانات الاعتماد وينجح فعليًا، لكن شيئًا ما يمنع الجلسة من الاستمرار.

فحص `server/_core/cookies.ts` كشف السبب الجذري: `getSessionCookieOptions`
كانت تُرجع **دائمًا** `sameSite: "none"`، بينما `secure` يُحسَب فعليًا
حسب البروتوكول (`http` → `false`). **`SameSite=None` بلا `Secure=true`
مزيج غير صالح وفق RFC 6265bis** — يرفضه أي متصفح حديث (Chrome يُطبِّق
هذا منذ 2020) تمامًا، ويتجاهل الكوكي بصمت دون أي خطأ ظاهر للمستخدم أو
للمطوّر.

### الأثر الحقيقي
هذا التطبيق يُقدِّم الواجهة والخادم من **نفس الأصل** (لا حاجة فعلية
لـ`SameSite=None` إطلاقًا — كانت على الأرجح قيمة منسوخة من مشروع آخر
يحتاج طلبات عابرة للمواقع). الأثر: **أي نشرة ذاتية الاستضافة لم تُفعِّل
HTTPS بعد** (بالضبط سيناريو مشروع ناشئ قبل ربط نطاق وشهادة SSL، أو حتى
أثناء التطوير المحلي كما حدث هنا) **تمنع كل تسجيلات الدخول بالبريد
الإلكتروني بصمت تام**.

### الإصلاح
```
sameSite: secure ? "none" : "lax"
```
بدل القيمة الثابتة السابقة. "Lax" آمن تمامًا وكافٍ لتطبيق أحادي المصدر،
ولا يتطلب `Secure=true`.

### التحقق
- **7 اختبارات وحدة جديدة** (`server/_core/cookies.test.ts`) تغطي كل
  التركيبات: HTTP عادي، HTTPS حقيقي، خلف بروكسي عكسي مع/بدون
  `X-Forwarded-Proto: https` صحيح.
- **تسجيل دخول حقيقي كامل بعد الإصلاح**: أُعيد بناء التطبيق، أُعيد
  تشغيل السيناريو نفسه — نجح تمامًا هذه المرة، والتنقل التالي أظهر
  `http://localhost:5099/dashboard` (تسجيل دخول فعلي ناجح ومستمر).

## الوصول الفعلي الأول لعارض الدرس واختبار الوحدة (بعد إصلاح الدخول)
بعد إصلاح خلل الكوكي، وصلت فعليًا (تسجيل دخول حقيقي، بيانات حقيقية
مزروعة: درس بمحتوى عربي طويل يحتوي معادلة رياضية، اختبار وحدة بسؤال
حقيقي) لعارض الدرس واختبار الوحدة على حجم هاتف (390×844):

- **عارض الدرس**: النص العربي الطويل (بما فيه أرقام ورموز رياضية
  لاتينية مُدمَجة: `2x + 4 = 10`) يُغلَّف ويُعرَض بترتيب bidi سليم تمامًا
  بلا أي تكسّر. لا تجاوز أفقي
  (`scrollWidth === clientWidth === 390` بالضبط).
- **اختبار الوحدة**: شريط تقدم، سؤال، وثلاثة خيارات إجابة مُنظَّمة في
  شبكة مناسبة للمس، زر "عرض النتيجة" بسهم اتجاهي صحيح. تحديد إجابة
  ("x = 7") يعمل بصريًا (حدود ذهبية واضحة على الخيار المُحدَّد). لا
  تجاوز أفقي هنا أيضًا.

**لم يُتحقَّق بعمق**: منطق تسليم الاختبار الدقيق (هل ينتقل فعليًا
لشاشة النتيجة عند الضغط على "عرض النتيجة") لم يُتابَع بالكامل في هذه
الجلسة — يُعتبر مُغطًّى بشكل كافٍ عبر اختبارات E2E الحقيقية المنفصلة
(`server/realDb.e2e.test.ts`) التي تختبر منطق التسليم والتصحيح فعليًا
عبر الموجّه الحقيقي، لا عبر واجهة المستخدم.

### التحقق النهائي بعد هذه الدفعة
- `pnpm run check`: ✅ صفر أخطاء.
- `pnpm test` بدون `DATABASE_URL`: ✅ 223 اختبارًا (+7 من اختبارات
  الكوكي الجديدة)، 18 مُسقَطة بصدق.
- `pnpm test` **مع** `DATABASE_URL` حقيقية: ✅ 238 نجح، 3 أُسقطت، صفر فشل.
- `DATABASE_URL=... pnpm test server/realDb.e2e.test.ts`: ✅ 18/18.
- `pnpm run build`: ✅ ناجح.

## ما لم يُنجَز بعد بهذه المنهجية (صريح)
- فحص بصري للوحة الإدارة الكاملة (`LearningFlows.tsx`) لخلل الأسهم —
  لم يُصلَح (أولوية أقل، مستخدمون داخليون فقط).
- بقية الصفحات لم تُفحَص بحجم هاتف تحديدًا (الدفع، اللوحة الرئيسية بعد
  تسجيل الدخول، الشهادة) — الإصلاح CSS عام لذا يُرجَّح أنه يُصلح كل
  الصفحات المشتركة في نفس بنية الرأس، لكن هذا **افتراض منطقي غير
  مُتحقَّق بصريًا لكل صفحة على حدة**.
- معاينة بصرية لصفحة الشهادة العامة (`CertificateVerify.tsx`) كصفحة
  ويب — بخلاف ملف PDF الشهادة الذي فُحص بصريًا في جلسة سابقة منفصلة.
- Lighthouse أو قياس أداء حقيقي على اتصال بطيء — لم يُختبَر توفره بعد.
- منطق تسليم الاختبار عبر الواجهة تحديدًا (وليس عبر الموجّه) لم يُتابَع
  بصريًا حتى شاشة النتيجة النهائية.

## متابعة ثالثة (نفس اليوم): فحص صفحة التحقق من الشهادة كصفحة ويب

### السياق
البند المتبقي من مراجعة الشهادة تحديدًا صفحتها كموقع ويب
(`CertificateVerify.tsx`) — بخلاف ملف الـPDF الذي فُحص بصريًا في جلسة
سابقة (واكتُشف وأُصلح فيه خلل انعكاس نص عربي).

### المنهجية
أُصدرت شهادة حقيقية عبر `issueCertificate()` الفعلية (بعد إنشاء تسجيل
`courseEnrollments` بحالة `completed` حقيقية فعليًا في قاعدة بيانات
حقيقية — الدالة ترفض الإصدار بلا هذا الشرط، كما تحقَّقتُ سابقًا في
Phase 1). فُتحت صفحة `/verify/certificate/{الرقم الحقيقي}` بمتصفح
حقيقي على ثلاث حالات: سطح مكتب بالعربية، سطح مكتب بالفرنسية، وحجم
هاتف حقيقي (390×844) بالعربية.

### النتيجة: لا خلل بصري جديد — تأكيد إضافي لإصلاحات سابقة
- **التصريح القانوني** ("ليست اعتمادًا رسميًا من وزارة التربية") ظهر
  بشكل صحيح تمامًا في كلتا اللغتين — تأكيد بصري إضافي (كان قد تحقَّق
  سابقًا في ملف PDF فقط).
- **سهم "العودة للرئيسية"** بالفرنسية صحيح اتجاهيًا (يشير لليسار، وهو
  الصحيح لزر "رجوع" في LTR) — تأكيد إضافي لنجاح إصلاح
  `DirectionalArrow` في صفحة لم تُذكَر بالاسم في التقارير السابقة رغم
  إصلاحها فعليًا.
- **لا تجاوز أفقي على الهاتف** (`scrollWidth === clientWidth === 390`
  بالضبط) رغم أن هذه الصفحة تحديدًا لم تُعدَّل مباشرة في إصلاح CSS
  السابق — دليل إضافي ملموس على نجاح شبكة الأمان العامة
  (`overflow-x: hidden` على `html`/`body`) في حماية صفحات لم تُفحَص
  بعد فرديًا.

### التحقق النهائي بعد هذه الدفعة
- `pnpm test` بدون `DATABASE_URL`: ✅ 223 اختبارًا، 18 مُسقَطة بصدق.
- `pnpm test` **مع** `DATABASE_URL` حقيقية: ✅ 238 نجح، 3 أُسقطت، صفر فشل.
- `DATABASE_URL=... pnpm test server/realDb.e2e.test.ts`: ✅ 18/18.
- `pnpm run build`: ✅ ناجح.

## الملفات المُعدَّلة عبر كل هذه الجلسات
`client/src/components/DirectionalArrow.tsx` (جديد)،
`client/src/index.css` (إصلاح التجاوز الأفقي + شبكة أمان عامة)،
`client/src/pages/{Home,CourseCatalog,CourseDetail,LessonViewer,Support,
Pricing,AlgorithmLab,LearningFlows,Auth,CertificateVerify,Dashboard,
Legal,Search}.tsx`، `server/_core/cookies.ts` (إصلاح حرج)،
`server/_core/cookies.test.ts` (جديد)، `PRELAUNCH_CHECKLIST.md`،
`PHASE_VISUAL_AUDIT.md` (هذا الملف).

لم تُترَك أي بيانات اختبار أو خوادم مؤقتة عاملة بعد انتهاء الجلسة.

## متابعة رابعة (نفس اليوم): Lighthouse حقيقي — واكتشاف خلل CSS بنيوي منهجي

### اكتشاف الأداة
تحقَّق أن **Lighthouse v13 حقيقي قابل للتشغيل فعليًا** عبر
`npx lighthouse` مُوجَّهًا لثنائي Chrome الحقيقي المُكتشَف سابقًا —
البند الأخير المتبقي في `PRELAUNCH_CHECKLIST.md` الذي كان يُفترَض أنه
"يحتاج أداة غير متوفرة" لم يعد كذلك.

### التشغيل الأول (قبل أي إصلاح)
`npx lighthouse` بمحاكاة أداء الهاتف (390×844، throttling-method=simulate)
على الصفحة الرئيسية:

| الفئة | النتيجة |
|---|---|
| الأداء | 68 |
| الوصولية | 88 |
| أفضل الممارسات | 96 |
| السيو | 91 |

### إصلاح فحوصات الوصولية الثلاث الأولى (سطحية، سريعة)
1. `maximum-scale=1` في `<meta name="viewport">` — يمنع المستخدمين من
   تكبير الشاشة، مخالفة وصولية حقيقية لمستخدمي ضعف البصر. أُزيل القيد.
2. تعارض `aria-label="Nourix Academy"` مع المحتوى المرئي الفعلي
   ("N" + "Nourix Academy") في رابط الشعار — أُصلح بإزالة `aria-label`
   وتعليم حرف "N" الزخرفي بـ`aria-hidden="true"`، وطُبِّق هذا الإصلاح
   بشكل متسق عبر **14 صفحة** تشترك في نفس نمط الشعار.
3. تباين لوني لعنصرين (`.section-kicker`, `.method-index`) — بدت
   القيم المُصلَحة كافية حسابيًا حينها.

### الاكتشاف الأكبر: خلل CSS بنيوي منهجي، وليس مجرد ألوان خاطئة
بعد الإصلاحات الثلاثة، أعاد التشغيل النتيجة: الوصولية ارتفعت إلى 95،
لكن **فحص تباين واحد ظل فاشلاً بقائمة عناصر أوسع بكثير من المتوقَّع**
(9 عناصر في `Home.tsx` وحدها). تتبُّع الألوان الفعلية المُقاسَة كشف أن
لون "السمة الفاتحة" الذي أصلحتُه للتو (`#7d6219`) كان يُقاس فعليًا ضد
**خلفية داكنة** (`#080808`) — أي أن لون السمة الفاتحة كان يتسرّب فعليًا
إلى السمة الداكنة!

**السبب الجذري**: قواعد CSS بالشكل:
```css
[data-theme="light"] .section-kicker, .method-index { ... }
```
الفاصلة هنا تُنشئ **محدِّدَين مستقلَّين تمامًا** في CSS — الجزء الأول
مُقيَّد بالسمة الفاتحة، لكن الجزء الثاني (`.method-index`) **غير مُقيَّد
إطلاقًا** ويُطبَّق على كل السمات.

**فحص آلي شامل للملف بأكمله** (سكريبت بايثون يحلّل كل قاعدة CSS، لا
regex سطحي) كشف **16 حالة إضافية** من نفس النمط بالضبط — نمط منهجي
متكرر في طريقة كتابة كل "تجاوزات السمة الفاتحة" في الملف، وليس خطأً
معزولًا.

**الأثر الحقيقي المُكتشَف**:
- إصلاح تباين كان قد **نجح فعليًا في المرحلة 3** (`.dashboard-course-row
  small`) انكسر فعليًا بسبب هذا التسرب — من 4.55 (صحيح) إلى 3.45
  (فاشل)، دون أن يُكتشَف وقتها لأن التحقق حينها اقتصر على حساب لون
  السمة الفاتحة بمعزل عن تأثيره على السمة الأخرى.
- قاعدة أخرى (`.dashboard-task`) كانت ستُظهر **خلفية فاتحة كريمية خاطئة
  تمامًا** (`#f2efe6`) داخل السمة الداكنة — خلل بصري فادح لم يُكتشَف
  إطلاقًا في أي مراجعة بصرية سابقة (لأن كل لقطات الشاشة السابقة رُكِّزت
  على السمة الافتراضية دون تبديل يدوي متكرر لكل عنصر).

### الإصلاح
سكريبت آلي (يحلّل كل قاعدة `selector-list { ... }` في الملف، ويُضيف
`[data-theme="light"] ` لأي جزء مفصول بفاصلة يفتقده عند وجود جزء آخر
مُقيَّد) أصلح **16 من 16 حالة حقيقية** دفعة واحدة. **تحقُّق فوري**:
`pnpm run build` نجح (يثبت صحة صياغة CSS)، وفحص آلي إعادي أكَّد **صفر**
حالات متبقية من نفس النمط.

### إصلاح 4 أخطاء تباين حقيقية منفصلة (غير مرتبطة بخلل التسرب)
بعد إصلاح خلل التسرب، ظهرت السمة الداكنة الحقيقية بألوانها الأصلية،
وكشفت **4 أخطاء تباين حقيقية ومستقلة** كانت موجودة أصلًا في السمة
الداكنة (`.code-window-bar small`, `.footer-brand p`, `.footer-lang
span`, `.footer-bottom`) — أُصلحت جميعًا بنفس منهجية حساب WCAG الدقيقة
المُستخدَمة في المرحلة 3.

### النتيجة النهائية (Lighthouse حقيقي، تشغيل ثانٍ كامل بعد كل الإصلاحات)

| الفئة | قبل | بعد |
|---|---|---|
| الأداء | 68 | 66 |
| **الوصولية** | 88 | **100** ✅ |
| أفضل الممارسات | 96 | 96 |
| السيو | 91 | 91 |

**فحوصات الوصولية الفاشلة المتبقية: صفر.**

### الأداء لم يتحسّن جوهريًا — سبب معروف وموثَّق بصدق
أكبر فرصة تحسين مُكتشَفة: **~410 كيلوبايت JavaScript غير مُستخدَم** في
حزمة `index-*.js` المشتركة (الصفحة الرئيسية لا تستخدم كل الكود المشترك
بين كل الصفحات الأخرى — أثر متوقَّع ومعروف لبنية SPA بحزمة vendor
مشتركة واحدة، وليس خطأً). تحقَّقت أن `recharts`/`framer-motion` (اللتان
حُذفت إحداهما والأخرى أُبقيت عمدًا في مرحلة سابقة) **لا تظهران** في
الحزمة المبنية فعليًا (0 تطابق بحث). تقليص هذا الرقم أكثر يتطلب إعادة
هيكلة أعمق لتقسيم الحزم (code-splitting على مستوى أدق من مستوى
الصفحة) — عمل هندسي أكبر وأخطر، **لم يُنفَّذ عمدًا** في هذه الجلسة
نظرًا لحجمه ومخاطره على بنية بناء تعمل حاليًا بشكل صحيح.

### التحقق النهائي
- `pnpm run check`: ✅ صفر أخطاء.
- `pnpm test`: ✅ 223 اختبارًا، 18 مُسقَطة بصدق.
- `DATABASE_URL=... pnpm test server/realDb.e2e.test.ts`: ✅ 18/18.
- `pnpm run build`: ✅ ناجح.
- **Lighthouse حقيقي، تشغيلان كاملان** (قبل/بعد) موثَّقان أعلاه بالأرقام
  الفعلية، وليست تقديرًا.

## الملفات المُعدَّلة في هذه المتابعة
`client/index.html` (إزالة قيد التكبير)، `client/src/index.css`
(إصلاح 16 قاعدة مُتسرِّبة + 4 ألوان تباين إضافية)،
`client/src/pages/{Home,Support,Dashboard,Pricing,Auth,CertificateVerify,
Notifications,Search,CourseCatalog,LessonViewer,LearningFlows,
CourseDetail,AlgorithmLab,Legal}.tsx` (إصلاح تكرار "N" الصوتي)،
`PRELAUNCH_CHECKLIST.md`، `PHASE_VISUAL_AUDIT.md` (هذا الملف).

لم تُترَك أي بيانات اختبار أو خوادم أو تقارير Lighthouse مؤقتة بعد
انتهاء الجلسة.
