# حالة المرحلة 3 — العربية وإمكانية الوصول

**تاريخ التحقق:** 2026-09-01 | **الحالة:** مكتملة جزئيًا بصدق (بعض البنود
مؤجَّلة صراحة أدناه، وليست مُعلَنة كمكتملة).

## منهجية العمل
فحص آلي ممنهج (بحث نصي + حساب فعلي لنسب التباين وفق صيغة WCAG) بدل
الاعتماد على انطباع بصري، مع تحقق type-check/tests/build بعد كل دفعة.

## الأخطاء الحقيقية المُكتشَفة والمُصلَحة

### 1. رموز تعبيرية (emojis) في لوحة الإدارة — 13 موضعًا
استبدال 🏅🎓📝🔴🟡⚪🖥️🌐⚠️✅🕵️📅🧾 بأيقونات `lucide-react` متسقة (`Award`،
`GraduationCap`، `FileCheck2`، نقطة ملونة بـ`aria-label`، `Server`،
`Globe2`، `AlertTriangle`، `CheckCircle2`، `History`، `Calendar`،
`Receipt`) في `Dashboard.tsx` و`LearningFlows.tsx`. أُبقيت الرموز
الطباعية البسيطة (✓ ✗ ↻ ✦) لأنها ليست "إيموجي" بالمعنى المقصود في
البرومبت، واستخدامها شائع في برمجيات احترافية.

### 2. labels غائبة عن نماذج حقيقية
- `Auth.tsx` (تسجيل الدخول/الحساب): كانت الحقول تعتمد فقط على
  `placeholder` بلا أي `<label>` — أُضيفت labels **مرئية** فعليًا
  (وليس فقط `aria-label`) مربوطة بـ`htmlFor`/`id`.
- `CourseCatalog.tsx`، `Search.tsx`: مربع البحث كان ملفوفًا في
  `<label>` بلا نص فعلي (أيقونة فقط) — أُضيف `aria-label` صريح.
- `Support.tsx`: حقول الموضوع/الرسالة/الرد كانت بلا اسم accessible —
  أُضيف `aria-label` لكل منها.
- `Pricing.tsx`: حقل كود الخصم كان يملك `<label>` مرئيًا لكن غير مربوط
  برمجيًا (`htmlFor`/`id` غائبان) — أُصلح الربط.
- **`CertificateVerify.tsx` كان بالفعل صحيحًا** (فحصته ووجدته يحتوي
  `aria-label` مسبقًا) — لا تعديل.

### 3. تباين ألوان حقيقي فاشل — النصوص الصغيرة الخافتة (`.quiet-label` وما شابه)
حساب فعلي (صيغة WCAG relative luminance) كشف أن اللون الافتراضي
لـ`.quiet-label` (وأصناف مشابهة تُستخدم بحجم 8-10px) كان **3.06:1 إلى
3.92:1** في السمة الداكنة — أقل من الحد الأدنى لـWCAG AA للنص العادي
(4.5:1). صُحِّحت 6 قواعد CSS (داكن وفاتح) بألوان أفتح/أغمق تحافظ على
الطابع الدافئ للتصميم مع بلوغ 4.5:1 فأكثر فعليًا (تم التحقق حسابيًا بعد
كل تعديل، وليس تخمينًا):
`.quiet-label` (داكن/فاتح)، `.dashboard-course-row/.dashboard-task small`
(داكن/فاتح)، `.staff-row small`، `.dashboard-task em`، `.next-meta`.

## ما رُوجِع ووُجد سليمًا مسبقًا (لا تعديل)
- `prefers-reduced-motion`: مُحترَم عالميًا بشكل صحيح مسبقًا.
- `:focus-visible`: مُطبَّق عالميًا على كل العناصر التفاعلية مسبقًا،
  إضافة إلى رابط "تخطي إلى المحتوى" (skip-to-content) موجود مسبقًا.
- فحص آلي شامل عن نصوص إنجليزية/فرنسية مختلطة أو منسية: **لم يُعثر على
  أي حالة حقيقية**. الحالات المشبوهة التي ظهرت أوليًا (مثل "points" في
  الفرنسية والإنجليزية، "Support" بالفرنسية) صحيحة لغويًا بالمصادفة
  (الكلمة نفسها في اللغتين)، وليست أخطاء. حقول إدخال الإدارة متعددة
  اللغات (`placeholder="Titre français"` إلخ) مقصودة أصلًا — كل دورة/
  درس/سؤال يحتاج إدخال العناوين الثلاث بالتزامن بغض النظر عن لغة واجهة
  الإداري، فالـplaceholder يحدد الحقل الصحيح ولا يمثل خطأ ترجمة.

## ما لم يُنجَز — مؤجَّل صراحة (وليس "مكتملًا")

### 1. اتجاه الأسهم (RTL) — خلل حقيقي مكتشَف، غير مُصلَح
الأيقونات الاتجاهية (`ArrowLeft` لـ"التالي/المتابعة"، `ArrowRight`
لـ"السابق") مُصمَّمة لتبدو صحيحة بصريًا في العربية (RTL) فقط، لكنها لا
تتغيّر ديناميكيًا حسب اللغة المختارة. **لمستخدم يختار الفرنسية أو
الإنجليزية (LTR)، تظهر الأسهم بالاتجاه المعاكس منطقيًا** (سهم "التالي"
يشير يسارًا بدل يمينًا). هذا النمط **مُستخدَم في عشرات المواضع عبر كل
الصفحات تقريبًا** (`Home.tsx`، `CourseDetail.tsx`، `CourseCatalog.tsx`،
`LessonViewer.tsx`، وغيرها) — إصلاحه بشكل صحيح يتطلب مكوّن/دالة مساعدة
تختار الأيقونة حسب `dir` الفعلي وتطبيقها في كل موضع، وهو **تعديل واسع
النطاق وعالي الخطورة إن نُفِّذ بشكل عشوائي دون معاينة بصرية شاملة لكل
موضع**. لم أُنفّذه في هذه الجلسة تفاديًا لمخاطرة كسر تصميم يعمل بصريًا
اليوم للغة الافتراضية (العربية) دون قدرة على التحقق البصري الفوري من
كل موضع. **يُوصى بجلسة مخصصة لهذا الإصلاح تحديدًا مع معاينة بصرية بعد
كل تعديل.**

### 2. labels نماذج الإدارة (~80 حقلًا في `LearningFlows.tsx`)
نماذج إنشاء/تعديل الدورات والوحدات والدروس والأسئلة والشارات والخطط
(لوحة المعلم/الإدارة) تعتمد على placeholders وصفية بلا `aria-label`
رسمية. هذه نماذج **داخلية لطاقم العمل وليست موجَّهة للجمهور العام**،
وحجمها كبير جدًا (~80 حقل عبر ملف واحد ضخم) — لم تُعالَج في هذه الجلسة
لضخامتها النسبية لأثرها المحدود (مستخدمون داخليون فقط)، وتُترَك كبند
مفتوح ذو أولوية أقل من النماذج التي يستخدمها المتعلم والزائر العام.

### 3. جدول/شهادة RTL بصريًا
لم تُجرَ معاينة بصرية فعلية (لقطة شاشة أو تصيير حقيقي) لصفحة الشهادة
أو الجداول في RTL — الفحص اقتصر على المراجعة النصية للكود. يُوصى
بمعاينة بصرية قبل الإطلاق.

### 4. ملاحظة تنظيف (اكتُشفت، خارج النطاق)
`client/src/components/AIChatBox.tsx` **غير مُستخدَم في أي مكان بالتطبيق
فعليًا** (لا يوجد أي `import` له) — كود متبقٍّ من قالب أولي غير مرتبط
بميزات NouriX Academy الفعلية. لم يُلمَس (لا قيمة لإصلاح accessibility
في كود لا يُعرَض أبدًا)، لكن يُنصح بحذفه في تنظيف لاحق لتقليل حجم الكود
غير المستخدم.

## التحقق النهائي (نفس هذه الجلسة)
- `pnpm run check`: ✅ صفر أخطاء.
- `pnpm test` بدون `DATABASE_URL`: ✅ 197 اختبارًا، 15 مُسقَطة بصدق.
- `DATABASE_URL=... pnpm test server/realDb.e2e.test.ts`: ✅ 15/15.
- `pnpm run build`: ✅ ناجح.
- نسب التباين: **مُحسَبة فعليًا بصيغة WCAG**، وليست تقديرًا بصريًا —
  كل قيمة مُصلَحة تم التحقق أنها ≥ 4.5:1 حسابيًا بعد التعديل.

## الملفات المُعدَّلة في هذه المرحلة
`client/src/pages/{Dashboard,LearningFlows,Auth,CourseCatalog,Search,Support,Pricing}.tsx`،
`client/src/index.css`، `PHASE3_STATUS.md` (هذا الملف).

لم يُحذف أو يُعَد تصميم أي شيء. لم يُنتقل إلى المرحلة 4.
